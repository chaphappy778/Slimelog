# SlimeLog Tracker — Living Document

*Single source of truth for active issues, technical debt, and action items.*

**Last Updated:** July 7, 2026  |  **Owner:** Jennifer Chapman

---

## Reference Docs (living)

Strategic and planning documents that inform work below. Refresh from these before starting a new work thread.

| Doc | Scope | Last touched |
| --- | --- | --- |
| [monetization-plan-2026-07-07.md](./monetization-plan-2026-07-07.md) | 8-pillar monetization strategy, revenue projections ($16-50M exit target), sequenced schema roadmap, realistic timelines with AI pairing. Read before starting any monetization-related work. | 2026-07-07 |
| [pre-launch-audit-2026-07-06.md](./pre-launch-audit-2026-07-06.md) | Full pre-launch security + code audit. 7 blockers ✅ shipped 2026-07-06; HP 8-14 ✅ shipped 2026-07-07; HP 15+ pending. Next up: HP 15 (input validation on /api/wishlist + /api/report). | 2026-07-07 |
| [schema-reference.md](./schema-reference.md) | Database schema reference. | see file |
| [hub-snapshot.md](./hub-snapshot.md) | Auto-generated snapshot from the StartupLenz hub. | see file |

---

## Status Legend

| Status | Meaning |
| --- | --- |
| READY | Available to work on now, no blockers |
| IN PROGRESS | In progress, has open work threads |
| BLOCKED | Waiting on external dependency |
| DONE | Shipped and verified |
| DEFERRED | Post-launch or lower priority right now |

---

## High Priority — Pre-Launch / App Store Blockers

| # | Issue | Priority | Status | Notes |
| --- | --- | --- | --- | --- |
| 14 | Subscription terms display | HIGH | READY | Apple requires auto-renewal terms on purchase screen. Part of #22. Unblocked 2026-07-14 when #22 unblocked. |
| 22 | RevenueCat integration | HIGH | IN PROGRESS | Unblocked 2026-07-14 — D&B address propagated, Apple Developer account approved and paid up under ChapHaus. James has set up a ChapHaus RevenueCat account and believes it's linked to the SlimeLog Stripe account; verification pass required. Remaining build: (1) create Pro tier product SKU in App Store Connect, (2) confirm RevenueCat ↔ Stripe cross-platform entitlement mapping (single Pro entitlement should honor both iOS IAP purchases + existing Stripe web subs so users don't double-pay when switching devices), (3) wire RevenueCat SDK into the Capacitor app once #23 is built, (4) sandbox purchase + restore flow testing, (5) subscription terms display copy per #14. 1 to 2 weeks of build once we start. |
| 23 | Capacitor packaging for iOS | HIGH | READY | Unblocked 2026-07-14 with #22. iOS build wrapper for the Next.js PWA. Blocks final RevenueCat SDK wiring. |
| 26 | App Store review checklist | HIGH | IN PROGRESS | Living pre-submission audit. External-dependency status updated 2026-07-14 — long-lead items all clear (D&B propagated, Apple Dev account paid + approved under ChapHaus, EIN + business entity + business checking + verified Stripe web all in place, ToS + Privacy Policy exist and will get a delta pass for App Store readiness). **Remaining checklist:** (a) **Sign in with Apple** — CONFIRMED REQUIRED because /login + /signup offer Continue with Google via Supabase OAuth. Apple mandates SIWA at equal visual weight anywhere 3rd-party SSO is shown. Build: add SIWA button (Supabase natively supports SIWA), configure Apple Services ID + key, wire callback through /auth/callback (existing PKCE route handles this). ~1 day of build. (b) App metadata pack — 1024x1024 master icon + Xcode auto-generation of variants, 6.7" iPhone screenshots (iPad if targeting), app description, keywords, support URL, marketing URL, privacy policy URL. 2 to 3 days design + copy. (c) Privacy Policy App Store delta pass — confirm every data type on Apple's nutrition label form is disclosed (auth identifiers, device identifiers, user content, contact info, usage data, diagnostics). 1 to 2 hours. (d) Privacy nutrition label form in App Store Connect — must match actual code, Apple cross-checks. 1 hour. (e) Age rating questionnaire — user-generated content lands at 12+ minimum. 15 min. (f) Support email support@slimelog.com with monitored inbox + Resend auto-reply promising 24h response (Resend already integrated per T41). ~30 min. (g) App Review Information: demo account credentials for Apple reviewers + notes on non-obvious flows (log wizard, rating slider, sharing). 30 min. Google Play deferred until Apple submission proves the flow (14-day tester gate on new Play developer accounts since Nov 2023 makes serial launches sensible). Realistic path 3 to 5 weeks from today to iOS submission gated by #22 build. |
| 33 | Password reset link broken | HIGH | DONE | 2026-07-10: root cause was PKCE code_verifier missing from browser storage — @supabase/ssr stores it in HTTP-only cookies accessible only server-side, but /reset-password was trying to exchange the code client-side. Fix: /forgot-password now sets redirectTo=/auth/callback?next=/reset-password?flow=recovery, /auth/callback short-circuits recovery flows by exchanging the code server-side then redirecting to the reset form with an established session. /reset-password recognizes ?flow=recovery + existing session as the ready state. |

---

## Medium Priority

| # | Issue | Priority | Status | Notes |
| --- | --- | --- | --- | --- |
| 4 | Push notifications | MEDIUM | READY | Drop alerts. Infrastructure not started. |
| 5 | Referral program | MEDIUM | DONE | 2026-07-10: shipped as combined feature with #30. Mig 62 (schema + code gen + backfill) + mig 63 (activation trigger + milestone rewards). ?ref= capture at signup + cookie fallback for OAuth. /invite dashboard + /i/[code] short-link. Milestones 5/25/100 → +1/+6/+12 Pro months, additive. Root-cause bug: /auth/confirm route was the actual email signup path, not /auth/callback — earlier fixes were on the wrong route. Also caught unrelated bug: /welcome page overwriting marketing_consent for email signups. |
| 16 | Data export (GDPR) | MEDIUM | DONE | 2026-07-10: /api/account/data-export returns a JSON download containing every row of user-owned data across profiles, collection_logs, brand_ratings, follows (both directions), brand_follows, comments, likes, comment_likes, notifications, activity_feed, slimes_created, brands_owned, brand_claims. Auth via session — never accepts a user_id from the request. Wrapped with _meta block (schema version + timestamp) for future export upgrade compatibility. Button in Settings → Privacy & Data. |
| 17 | Marketing consent flag | MEDIUM | DONE | 2026-07-10: mig 61 adds profiles.marketing_consent + marketing_consented_at (default false, GDPR opt-in). Captured on /signup + /welcome + /settings toggle. Brevo sync via syncContactMarketingConsent(). Enforcement comment in lib/brevo.ts requires all marketing routes to filter marketing_consent = true. |
| 19 | In-feed advertising | MEDIUM | DEFERRED | Free tier only. Defer post-launch. |
| 20 | Double toast on Pro upgrade | MEDIUM | DONE | 2026-07-10: root cause was the upgrade-toast useEffect re-running on remount (React 18/19 strict mode double-invoke in dev, potential remount edge cases in prod). Fix: ref-guarded fire in /settings/profile so the toast can only ever surface once per page-load regardless of remount cycles. |
| 21 | Duplicate subscriptions check | MEDIUM | DONE | 2026-07-10: /api/stripe/checkout now queries Stripe directly when the DB says "not active" — catches the case where subscription_status has drifted stale (e.g., HP-8 webhook update silently dropped). If Stripe shows a live active/trialing sub for the customer, we route to billing portal instead of creating a duplicate + reconcile the DB row using the found subscription's status + item.current_period_end. |
| 25 | Pro upgrade button in SlimeMenu | MEDIUM | BLOCKED | Deferred with RevenueCat. |
| 28 | Search | MEDIUM | READY | Global search across slimes, brands, users. |
| 29 | In-app notifications | MEDIUM | DONE | 2026-07-12: shipped notification bell + /notifications feed reading from `public.notifications`. New routes: `GET /api/notifications` (RLS-scoped, polymorphic joins on actor/brand/drop/log, `?before` cursor + `?unread=true` filter), `POST /api/notifications/mark-read` (`{ ids }` or `{ all: true }`, recipient-locked), `GET /api/notifications/unread-count` (bell polling). Components: `components/notifications/NotificationBell.tsx` (line-SVG bell + magenta badge, hidden signed-out + on `/notifications`, polls every 60s + on window focus), `components/notifications/NotificationsFeed.tsx` (paginated list, mark-all-read, geometric empty state, skeleton loading), `components/notifications/NotificationRow.tsx` (renders all 10 enum types with icon + copy + link, graceful fallbacks when a joined relation is null, unread rows get a cyan left-border accent). Bell mounts in `PageHeader.tsx` between profile avatar and hamburger. `formatRelativeTime` extracted from `/brands/[slug]/page.tsx` into `lib/format-time.ts`. Only 2 of the 10 enum types actually get INSERT'd today (`brand_suggestion_approved` + `brand_suggestion_rejected` from admin approve/reject routes) — the remaining 8 are future-ready (T29b). Realtime push deferred (T29a). |
| 30 | Sharing | MEDIUM | DONE | 2026-07-10: shipped alongside #5. `<ShareButton>` component (native Web Share API on mobile, clipboard fallback on desktop) on slime detail / drop detail / brand / user profile / /profile pages. Auto-appends signed-in user's `?ref=CODE` so every share doubles as an invite link. |
| 31 | Onboarding | MEDIUM | DONE | 2026-07-10: 4-screen full-screen modal on first login (Welcome / Log your first slime / Rate what matters / Invite friends). Gated by profiles.onboarding_completed_at (mig 64). OnboardingGate mounts on / (feed) and only renders for users with null completion timestamp. Skip and Complete both stamp completion via /api/account/onboarding-complete. Step position persisted to localStorage (survives refresh). All visuals geometric — no AI art per community sensitivity. Referral code + milestone ladder on screen 4 wires into the referral system. |
| 32 | Slime type guide pages | MEDIUM | DONE | 2026-07-13. Shipped **The SlimeLog Guide** at `/guide` — the full 12-part community reference per Jenn's V4.1 approved copy in `docs/slime_guide_v41_extracted.txt`. Server-rendered so the whole guide is in HTML for SEO. New route `apps/web/app/guide/page.tsx` renders all 12 parts + `content.ts` module holds the structured data (all 20 textures + add-ins + containers + scents + brand glossary + care + safety + pricing + shipping + aesthetic vocab + sound vocab + rating framework). Old `/slime-types` route replaced with a `redirect("/guide")` so bookmarks and inbound links transparently forward. SlimeMenu "Learn" section link updated to `/guide` labeled "The SlimeLog Guide". New components under `apps/web/components/guide/`: `GuideNav` (sticky pill row with scroll-spy + hamburger TOC drawer), `TextureExplorer` (client — 2-col gradient tile grid + bottom sheet, handles hash-on-load for `/guide#texture-butter`), `GlossaryList` (parts 2/4/10/11 term/tag/definition rows), `ProseSection` (parts 6/7/9/12 reading-column with tone-tinted callouts), `BrandGlossary` (part 5 grouped by product/sales/makers/care/community), `PricingBands` (part 8 glowing glass rows with green→cyan gradient value column). Deep-linkable anchors: `/guide#part-6` scrolls to Part Six, `/guide#texture-butter` scrolls to butter card AND opens its detail sheet on load. Log counts for Part One CTAs computed server-side via a single `collection_logs.base_type` aggregate — swap to RPC/matview when volume warrants (docs/cost-tracker.md). Real photos live in `apps/web/public/guide/textures/` and are keyed to specific example brands per texture (sand + magnetic ship gradient-only for now — sand has no photo, magnetic keeps `crazy-aarons`). Anti-AI-art hard rule respected — real photos + geometric decorations only. |
| T32a | Guide Part 1 art-direction pass | LOW | READY | 2026-07-13. Once /guide ships and we see it live, revisit the texture-tile treatment: photo/gradient opacity balance, whether the wave decoration should appear on all tiles or only the gradient-only fallbacks (sand), consistent typographic framing across all 20. Fast iteration once we've seen it in the wild. |
| T32b | Featured Shops row atop Guide Part 5 | MEDIUM | DONE | 2026-07-13. Shipped `FeaturedShopsStrip` above Jenn's vocab glossary. Server-side aggregate on `collection_logs.brand_name_raw` (top 10 by log count, case-insensitive) + per-brand ILIKE catalog lookup for slug/logo_url. Each tile links to /brands/[slug] when the brand is catalogued; free-text brands render as non-clickable name tiles with hash-color initial fallback. "See all →" link to /leaderboard. Log count + "See all" both bake into the strip so Part 5 now doubles as the community-discovery entry into brand pages. Same in-memory aggregation cost as leaderboard — see docs/cost-tracker.md for the mitigation path when volume warrants. |
| T32c | Guide Part 3 sizes chip strip | LOW | DONE | 2026-07-13. Replaced the `ContainerGrid` treatment for `CONTAINER_SIZES` with a horizontal `SizeChipStrip`. Each chip renders the size (4oz/6oz/8oz/16oz/32oz) in Montserrat black with a green→cyan gradient, description as a sub-line inside the chip. Reads tighter than a two-column grid for a five-item numeric ladder. |
| T32d | Reconcile /guide Part 12 vs /how-to-rate | MEDIUM | DONE | 2026-07-13. /guide Part 12 was describing a legacy 9-dimension / 10-point rating system (Texture / Stretch / Hold / Drizzle / Sound / Scent / Inclusions / Packaging / Value) — this predated the six-axis / five-star model the app actually uses. Retired that prose block. Part 12 now renders a lightweight `AxisIndex` component defined in `apps/web/app/guide/page.tsx` that maps `RATING_AXES` from `apps/web/app/how-to-rate/content.ts` into six axis cards: two-digit number badge in the axis accent color, axis name (gradient text for Overall), tagline, and chevron all colored per axis. Each card is a `Link` to `/how-to-rate#{slug}` so tapping an axis lands the user on that section of the how-to-rate deep dive. Bottom CTA "Read the full rating guide" (green→cyan gradient) still links to `/how-to-rate`. Source of truth for axis names / colors / order is now the how-to-rate content module — no duplication, no drift. Also removed the orphaned `RATING_FRAMEWORK` export from `guide/content.ts`. Type-check clean. |
| T32e | Redesign /how-to-rate to match guide layout | MEDIUM | DONE | 2026-07-13. Shipped the /how-to-rate redesign as a server-rendered sibling of /guide. Same URL, no redirect. Route: `apps/web/app/how-to-rate/page.tsx` rebuilt from a 776-line client component into a lean server component that composes new components from `apps/web/components/how-to-rate/`. New content module at `apps/web/app/how-to-rate/content.ts` — `RATING_AXES` (six axes: Texture / Sound / Aesthetic / Creativity / Quality / Overall with per-axis gradientFrom, gradientTo, accentColor, and optional accentGradient for gradient text), `SCALE_BANDS` (five bands: Skip / Under / Solid / Great / Elite with per-band accent color), `PARTS` (seven entries for pill nav + TOC). All copy is Design's approved mockup, em-dash-free. New components: (a) `HowToRateNav.tsx` forked from `GuideNav` — sticky pill row with JS-driven fixed pinning, scroll-spy across the seven parts, auto-scroll of active pill, `left: 6, right: 6` alignment when pinned to match PageWrapper's `px-1.5`, triple `scrollTo(0,0)` + `history.scrollRestoration = 'manual'` scroll-restore hardening, hash-on-load supports `#texture..#the-scale`. (b) `HowToRateTOCDrawer.tsx` — hamburger side sheet listing all seven parts. (c) `AxisSection.tsx` — per-axis body: two-line section header, big rounded-3xl hero card (radial accent glow over linear gradient from gradientFrom→gradientTo, number badge top-left, 3-stroke wave decoration top-right, axis name bottom-left), Definition prose, "What to look for" bullets with glowing accent dots, LOW / HIGH score examples with `LowHighBar` visualizations, optional magenta note callout, magenta `See top-rated slimes on {axis} →` deep-link to `/discover?sort={slug}`. (d) `LowHighBar.tsx` — horizontal rounded pill with ~25% magenta fill for LOW, 100% green fill with glow for HIGH. (e) `ScaleSection.tsx` — "07 The Scale" section with a full-width red→amber→cyan→green gradient scale bar, five band rows (44px numbered chip in the band's accent color + star row + name + prose), and two closing callouts: magenta "What a 5.0 means" + amber "When to withhold a rating" (matches ProseSection warn treatment — caution icon + glowing dot flanking the label). Hero + footer CTA: gradient "How to Rate a Slime" headline (green→cyan→magenta gradient span on "a Slime"), "Read the full SlimeLog Guide →" back-link to `/guide#part-12`, footer green→cyan gradient "Log a slime and rate it →" CTA to `/log`. SEO: `generateMetadata` with title / description / OG image (butter-cats-craft.webp). Anti-AI-art respected — line SVG icons (2px stroke) + gradient decorations + typography only, no character mascots. Type-check clean. |
| T33 | Discover V1 rework | HIGH | DONE | 2026-07-13. Shipped Design's Discover Evaluation V1 slate. New section order: Search hero → Trending pulse (or early-days empty state) → Slime Types → Suggest a brand card (relocated from top) → Trending Keywords (split into its own section) → Top Rated Slimes (medal tiles for top 3) → Popular Collectors (with substance line) → Upcoming Drops (with T-minus pills). New components: `SearchHero.tsx` (full-width cyan-glow input, rotating placeholder, routes to `/search?q=<text>` on Enter — typeahead is V2), `TrendingPulse.tsx` (big-number logs today + 7-bar sparkline + up to 3 momentum rows for climbing base types; server-side aggregation over `collection_logs` last 8 days), `TrendingPulseEmpty` (seed-the-community empty state, gates on `EARLY_DAYS_THRESHOLD = 20` so pre-launch it renders instead of a sad pulse widget). Modifications: `PopularUsersCarousel.tsx` redesigned from small avatar strip to full 156px cards with `favorite_base_type` (mode of user's logs, requires ≥ 3 logs before we call anyone a specialist), `slime_count`, `avg_rating_given` — computed via a single per-batch enrichment query joined to `slimes.base_type`. `FeaturedDropsCarousel.tsx` gained `computeTminus` + `tminusStyle` — every drop card now carries a top-right pill: LIVE (green→cyan gradient) if `status === "live"`, gold "T-Nh/d" if under a week, cyan "T-Nw" if further out. `DiscoverSlimesClient.tsx` `RankBadge` now uses `MEDAL_STYLES` — rank 1 green→cyan (matches primary CTA), rank 2 silver gradient, rank 3 bronze gradient, 4+ muted purple. Design's proposed "For you" strip intentionally deferred pending user volume + collab-filtering data. Bell / notifications inbox not added (no notifications system live). Search hero + typeahead noted as V2, LIVE-drop interrupt band + full per-type color system also V2. Cost tracker entries filed for pulse feed + collector enrichment queries. Type-check clean. |
| T32f | Wire /discover?sort=&lt;axis&gt; deep-links | LOW | DONE | 2026-07-13. `/discover` now accepts `?sort=<axis>` for `texture / sound / aesthetic / creativity / quality / overall`. Server (`apps/web/app/discover/page.tsx`): added `AXIS_TO_COLUMN` slug → DB-column map, `normalizeAxisSlug` guard (unknown values fall back to `overall`), swapped the top-rated query's `.not("avg_overall", "is", null)` + `.order("avg_overall", ...)` to use the axis-mapped column, and updated the section label to "Top Rated by <Axis>" when the sort is customized. Passes the axis as a typed `SortAxis` prop into `DiscoverSlimesClient`. Client: `readRating` helper resolves the axis column to a value; `filtered` `useMemo` sorts by that column when `sortMode === "top_rated"` and `minRating` filter uses it too, so "min 4+" on a texture sort means "min 4+ on texture." `TopRatedCard` gained a `ratingValue` prop so its `RatingBar` and readout show the axis value (e.g. avg_texture) rather than avg_overall. New "Sorted by <Axis>" cyan chip above the filter bar with a magenta "Clear" link back to `/discover`. Cost tracker entry filed — five unindexed axis columns will need btree indexes once `slimes` grows past tens of thousands of rated rows. Type-check clean. |
| T34b | Edit-log wizard visual parity + shared wizard tokens | MEDIUM | DONE | 2026-07-13. `/log/edit/[id]/page.tsx` was still on the old wizard chrome (dot-row StepIndicator, `<select>` base-type picker, plain `Field` wrappers, rating stack without per-axis cards) even though T34 rebuilt `/log/page.tsx`. Jenn caught it during the wizard smoke — ratings + notes felt new but Identity + Details felt like the old app. Ported the same visual rebuild to the edit page. New shared module `apps/web/components/log/LogWizardShared.tsx` exports the wizard tokens both pages need: `RATING_FIELDS` (extended per-axis metadata), `COLOR_SWATCHES`, `sectionCardStyle`, `fieldInputStyle`, `FieldLabel`, `PickChip`, `ToggleKnob`, `SummaryRow`, `RatingAxisCard`, `ColorPicker`. Both `/log/page.tsx` and `/log/edit/[id]/page.tsx` now import from this shared module; any future wizard visual change lives in one place. Edit page return block replaced with the same JSX as create page — sticky `LogStepHeader` + sticky `LogStepFooter` + Identity step with photo tile + `BaseTypePicker` carousel + Details step with `PickChip` scent/condition rows + Ratings step with six `RatingAxisCard` per-axis tinted cards including Overall rainbow border + Notes step with public/private toggle card + summary preview. Edit page keeps its own `handleSubmit` (UPDATE via Supabase, routes back to `/slimes/[id]`) and its own initial-load `useEffect` that hydrates form state from the existing collection_log row. Local `StepIndicator` / `Field` / `ColorPicker` / `inputCls` / `cardStyle` / `RATING_FIELDS` / `COLOR_SWATCHES` / `KNOWN_COLOR_VALUES` (moved out and re-derived from shared module) all retired. Type-check clean. |
| T37 | Social stats on profiles + follower/following lists | MEDIUM | DONE | 2026-07-13. Public profile at `/users/<username>` already displayed follower + following counts but they weren't clickable — no way to see WHO. Own dashboard at `/profile` didn't have the counts at all. This pass adds both. (1) `/profile` server page fetches follower + following counts via two extra head+count queries against `public.follows`. New "Social" section renders above "Collection Stats" as two clickable glass tiles: cyan-numbered Followers + magenta-numbered Following. Both link to the public list pages using the user's own `rawUsername`. (2) `/users/<username>` counts wrapped in `<Link>` so they navigate to the same list pages. (3) New route `/users/<username>/followers/page.tsx` — server component: resolves target by case-insensitive username, fetches followers via `follows` joined to `profiles_public` (RLS lets anon read the follow graph and public profiles), pre-computes viewer's own follow-state for each row in a single batch IN() query, renders header ("N followers of @username") + list of `FollowListRow` items. (4) Mirror route `/users/<username>/following/page.tsx` with opposite join direction ("@username follows N collectors"). Both include a Back to `@username` link + orbs-enabled `PageWrapper`. (5) New shared component `apps/web/components/profile/FollowListRow.tsx` — 44px avatar (green ring if verified) + `@username` + optional bio snippet + `FollowUserButton` (hidden when viewer looks at their own row). Whole row body is a `Link` to the profile except the Follow button which owns its click. (6) Supabase's typegen returns 1:1 joins as arrays — normalized via `Array.isArray()` check in both list pages. Likes + comments counts NOT added — deferred to whenever we ship the log_likes / log_comments schemas (see T33d for likes). Type-check clean. |
| T36 | Auto-follow seed accounts on signup | MEDIUM | DONE | 2026-07-13. Every new user is silently auto-follows a curated set of seed profiles when their `profiles` row is created (MySpace-Tom pattern). Kills the day-1 empty-feed problem and gives us a broadcast channel to every new user. New migration `20260713000071_seed_follows_bootstrap.sql`: (1) new `public.seed_follow_accounts (profile_id primary key references profiles, note, created_at)` config table with RLS gate — anon+authenticated can read (seed status isn't sensitive), writes gated to service role only so admins manage via SQL editor. (2) Seeds `slimelog_official` and `jenn_slimelogapp` via username lookups (safe across environments; no-op INSERT if the username doesn't exist yet). (3) `bootstrap_new_user_follows()` SECURITY DEFINER function inserts follows from the new profile → each seed via `INSERT ... SELECT s.profile_id FROM seed_follow_accounts s WHERE s.profile_id <> new.id ON CONFLICT (follower_id, following_id) DO NOTHING`. Skip-self clause prevents Jenn's profile from auto-following herself on the initial seed. (4) `trg_bootstrap_new_user_follows AFTER INSERT ON profiles FOR EACH ROW`. Trigger reads the seed table at runtime so admins can add/remove seeds without a code redeploy. Backfill of existing users deliberately NOT run per Jenn 2026-07-13 (only one other user pre-launch, already follows Jenn). Backfill SQL left commented at the bottom of the migration for future seed additions we want retroactive. Deploy: `npx supabase db push` before user signups after this ships. |
| T41 | Video reviews (post-launch, gated on data) | MEDIUM | READY | 2026-07-13. Slime IS a video medium (ASMR clips, drizzle shots, cutting sounds) — competing with TikTok on their turf without video means we're hobbled. But raw storage + bandwidth math kills a naive implementation. This ticket = the actually-affordable version. Build spec: (1) Upload cap 15 sec, 720p vertical, ~4 MB target after compression. Server-side transcode via ffmpeg on a worker OR Bunny Stream / Cloudflare Stream's transcode-on-ingest. (2) Tap-to-play only. No autoplay. Static cover thumbnail auto-generated from the 1st frame. TikTok autoplays because it's their revenue model; we don't need to and it cuts bandwidth ~4x. (3) CDN in front of storage — Cloudflare CDN over Supabase Storage OR fully-managed Bunny Stream ($1/1000 min viewed). Cloudflare CDN with ~70% cache hit gets bandwidth cost to ~$130/mo at 5k DAU. Bunny Stream costs ~$4500/mo at same volume but zero infra work. (4) Video log gets a "▶" glyph overlay on its cover thumbnail so users can tell photo vs video at a glance. (5) Reuse the existing `image_url` column pattern — add `video_url text NULL` + `video_thumbnail_url text NULL` + `video_duration_ms int NULL` on `collection_logs`. (6) Feed / log detail / brand detail / Discover all render video cards the same as photo cards, just with the play glyph. Gate this ticket on real user data post-launch: what % of active users upload video (guess 20-30% of loggers), avg watch time per DAU, whether the flywheel justifies ~$500-1000/mo infra spend at scale. Realistic 2-3 week build once green-lit. Cost tracker entry filed at build time. |
| T40 | Share-to-social deep-link flow | HIGH | READY | 2026-07-13. Growth-critical companion to T39 (tag flow audit). Ships the "middle-ground" cross-post pattern most cross-post tools actually use since Meta and TikTok's Content Posting APIs are heavily gated (Meta Graph API requires Business/Creator accounts + multi-week app review; TikTok Content Posting API is manually approved and hard to get). Build spec: user taps "Share to Instagram" (or TikTok) on `/slimes/<id>` — client-side flow: (1) auto-downloads the log's photo to the phone's Photos, (2) auto-copies a pre-formatted caption to the clipboard (brand handle tagged + rating + slime name + link back to SlimeLog log with UTM), (3) opens the native app via deep link (`instagram://library` / `snssdk1233://` etc where possible; fall back to `https://instagram.com` in the browser). User just taps Post — everything's pre-filled. Saves Jenn ~45-60 sec per post × 2-3 posts/day × 2 apps = ~5-8 min/day; scales to every other user doing the same. Works without either platform's API approval — pure client-side. Caption formatting: `${slime_name} ${rating}/5 · @${brand_handle} · slimelog.com/s/${slug}` or similar (Jenn to spec). Full-API auto-publish version (Meta Graph API + TikTok Content Posting API) filed as T40a for post-launch when we have volume to justify the review effort. |
| T39 | Tag-flow friction audit (pre-launch) | HIGH | READY | 2026-07-13. Growth-critical: Jenn's brand-tagging on Instagram is the primary flywheel driving waitlist growth (13 → 42 in a month). Every log-with-brand-tag → brand notification → brand reshare → new signup. Audit the full tag path in the app and eliminate every point of friction before launch. Checklist: (1) BrandSearchInput on the log wizard — does it typeahead fast? Does "not found" flow cleanly into Suggest a Brand? (2) When a log saves with a brand_id, does the brand OWNER see it in their dashboard notifications? Do they get an email? (3) The Share button on `/slimes/<id>` — does it pre-fill an IG/TikTok share caption with the brand's handle + a link back to the SlimeLog log? (4) Does the log detail page open graph image include the brand name + logo prominently enough that a reshared link looks native on IG? (5) Any brand claim / verification friction after they see the log — does it take more than 60 seconds for a brand to claim and start responding? Each of these is a leak in the funnel. Fix any that fail before launch. |
| T38 | Brand detail page redesign + drops tubs inventory | HIGH | DONE | 2026-07-13. Shipped `/brands/<slug>` per Design's brand-detail pack (`docs/handoffs/2026-07-13-brand-detail-page-brief.md`; mockup zip at `outputs/brand-detail/`). New migration `20260713000073_drops_tubs_available.sql` adds optional `tubs_available int` column on `public.drops` for brand-set inventory hints (brand updates via dashboard, no real-time sync). Rebuilt view `public.upcoming_drops` to project the new column through. Brand detail page (`apps/web/app/brands/[slug]/page.tsx`) rewritten: (1) 200px banner with bottom scrim + 3px magenta pro-hairline at top for `subscription_tier = 'brand_pro'` shops. Banner uses `banner_url` when set or falls back to the signature purple radial. (2) 72px rounded-square logo overlaps the banner bottom; magenta glow for brand-pro, green→cyan glow for free. (3) Name row: Montserrat 900 25px name + green-circle-white-check verified badge (retires the old cyan 5-point star) + Follow / Manage action on the right — owner sees a green→cyan gradient Manage button linking to `/brand-dashboard/<slug>`, others see `FollowBrandButton`. (4) Meta row: location + restocks schedule with line-SVG icons (only when set). (5) Bio prose. (6) Inline stats row framed by hairline rules top/bottom — star + rating + count · truck + logs · people + followers. Retires the boxy pill panels. (7) Socials row — line-SVG icons circled, brand-pro tinted magenta as their signature reward, free brands cyan. (8) Action row: full-width "View slime catalog →" secondary button + Share icon button, followed by optional `ClaimBrandButton` when the user could plausibly claim. (9) Upcoming drops section — dense glass rows with 78x78 cover + T-minus pill (LIVE = green→cyan gradient + glow, T-Nd/h = cyan glass) + drop name + date · tubs_available line (only when brand set the field). LIVE drops get a magenta border + magenta glow signal. Empty state: illustrated glass card with follow prompt. (10) Community logs — reuses the log-card visual from `/discover/type/<base_type>` (172px cover + cyan rating pill top-right + avatar footer with username + slime name + relative time), capped at 6 with a "showing X of Y" note. Empty state: illustrated glass card + "Log a slime" green→cyan CTA. (11) Top collectors strip — kept `TopCollectorsStrip` component intact; re-skinned the section header only. Deferred per brief + review: "Pro shop" pill dropped (paid-placement label contradicts brief), "Log it" and "Remind me" drop-card CTAs dropped (new interactions, backend work), owner Edit-brand affordance shipped as "Manage" button linking to existing `/brand-dashboard/<slug>` (Design flagged as not-in-this-round; we already ship the dashboard so we wired it). Type-check clean. Push order: migration first (`npx supabase db push`), then code. |
| T35 | Brands page rebuild | HIGH | DONE | 2026-07-13. Shipped `/brands` per Design's mockup pack (`docs/handoffs/2026-07-13-brands-page-brief.md`; mockup zip at `outputs/brand-page/`). Server `apps/web/app/brands/page.tsx`: added a fifth query for the "N new this week" hero pill (`brands.created_at >= now() - 7 days`, count only), merged the previous Verified + Community query results into a single `allBrands` array server-side so the client renders one unified list. 4-tier query structure (Featured / Popular / Verified / Community) preserved server-side — only the client presentation is consolidated. Loud `console.error` on each query error for silent-empty debugging. `BrandsClient.tsx` full rewrite: retired the old filter sheet + toggle row + split Verified/Community sections. New surface: big Discover-style header (`DISCOVER` cyan eyebrow + `Brands` Montserrat 900 42px + stats sub `266 shops · 70 verified` + optional green "N new this week" pill), then Featured shop card, Popular shops horizontal carousel, then unified `All brands` section (cyan-glass search input with clear-X, horizontal scroll sort chip row `Top rated / Most logs / Followers / A–Z`, divider, `Verified only` cyan toggle chip, 2-column grid), then Suggest a brand card. `FeaturedBrandCard.tsx` full rewrite: 120px cover gradient at top with scrim, 66px rounded-square logo (real photo or gradient initials) overlapping the cover, name in Montserrat 900 22px with green check, rating/followers/logs stats row with line-SVG icons (star fill, person, chat), tagline from `brand.bio ?? brand.description`, optional cyan restock-schedule callout pill with clock glyph, full-width green→cyan Montserrat 900 Visit shop CTA. `PopularBrandsCarousel.tsx` full rewrite: horizontal scroll of 200px cards, each with 74px cover gradient + 44px logo overlapping bottom-left of cover, name + green check + rating (green Montserrat 800) + follower count. "Most logged" mini-tile deferred to T35a. `BrandCard.tsx` full rewrite: compact grid card with 54px rounded-square gradient logo (or real photo), name + check, rating with star or `No ratings yet`, followers with icon, all center-aligned. New shared `apps/web/lib/brand-gradients.ts` exports `brandLogoGradient(id)` + `brandCoverGradient(id)` + `brandInitials(name)` — deterministic per brand id so a shop always renders the same signature colors. Palette matches Design's mockup (green→cyan, magenta→cyan, cyan→green, warm→pink, holo, deep purple, etc). Emojis from Design's mockup (🚨 in drop callout, 🧈/🌌/🧊 in Most Logged tile) intentionally stripped per SlimeLog no-new-emoji rule; drop pill uses a clock line-SVG instead. Suggest a Brand card lives on this page (relocated from /discover on 2026-07-13). Type-check clean. |
| T35a | Popular Shops "Most Logged" sub-tile | LOW | DEFERRED | 2026-07-13. Design's mockup for the Popular Shops carousel includes a MOST LOGGED mini-tile on each card showing the brand's top-logged slime name + a small icon. Requires a per-popular-brand aggregation: `for logs in collection_logs where brand_id in (popularBrandIds), group by (brand_id, slime_id), pick top-1 per brand`. Doable via one batch query + JS reduce (same pattern as popular-collectors enrichment on Discover). Deferred from V1 because (a) pre-launch we have essentially no popular brands with logged slimes, (b) the mini-tile is a nice-to-have signal not a critical affordance. `PopularBrandsCarousel.tsx` header comment notes exactly where to slot the tile when we build. |
| T34 | Log wizard visual redesign | HIGH | DONE | 2026-07-13. Shipped the log-wizard rebuild per Design's mockup pack (`docs/handoffs/2026-07-13-log-wizard-look-brief.md`; mockup zip at `outputs/wizard-look/`). Form state + `logSlime` action + FormState interface all UNCHANGED — only the visual shell and step layout are new. New components under `apps/web/components/log/`: (1) `LogStepHeader.tsx` — sticky pill row (4 numbered circular pills with connector segments; active cyan bg + cyan border + cyan glow + inset glow, done green fill + black checkmark + green glow, muted purple outline + muted white text), eyebrow "STEP 0N" in cyan Montserrat 800 uppercase 0.16em letterspacing, big Montserrat 900 title (`What slime is this?` / `Tell us more` / `Rate it` / `Any notes?`), rainbow gradient title for step 2. (2) `LogStepFooter.tsx` — sticky Back/Next (Cancel/Save on edges), primary green→cyan gradient Montserrat 900 CTA with green glow + cyan drop shadow, secondary muted purple card treatment, disabled + "Saving..." states. (3) `BaseTypePicker.tsx` — horizontal scrolling photo carousel (144×168 cards) using `BASE_TYPE_HERO_PHOTO` from `lib/base-type-hero.ts` (same source as Discover TypeCarousel), selected state = magenta 2px border + magenta glow + magenta "SELECTED" eyebrow + magenta checkmark circle top-right. `RatingSlider.tsx` fully rebuilt: 12px-tall track showing the how-to-rate five-stop scale (`#FF3D6E → #FF7A2E → #00A6FF → #00E28A → #39FF14`) as background, semi-opaque dimmer over the UNFILLED portion (Design's contrast trick), 22px white handle with 4px band-colored ring + 18px band glow at 44px tactile target, band-colored 26px Montserrat 900 numeric readout in top-right, rainbow-gradient label on Overall. Star row: five outlined line-SVG stars below track, each fills proportionally via SVG clipPath (Design's "partial fill" / clip fractional style default). Hidden native `<input type="range">` drives value with arrow-key stepping + aria-valuenow. Wizard shell rewrite in `apps/web/app/log/page.tsx`: retired old `StepIndicator` + `cardStyle` + PageWrapper hero card + FloatingPills. New per-axis rating card (`RatingAxisCard`) with `1px solid ${accent}66` border + inset accent glow for the five per-axis cards; Overall gets a two-layer gradient border trick (rainbow border-image + inner purple fill) + "Almost done, final call" eyebrow. Photo tile moved to Identity (step 0) per Design's Q3 (identify + photograph in one pass); other fields keep step. New extracted helpers: `FieldLabel` (Montserrat 800 cyan uppercase with magenta * for required, muted "optional" tag inline), `fieldInputStyle` (glass field treatment), `sectionCardStyle` (canonical `rgba(45,10,78,0.3)` glass card), `PickChip` (chip toggle with settable tint for scent/condition), `ToggleKnob` (green→cyan when on), `SummaryRow` (Notes step summary card). Ratings mode wired for `scroll` only in V1 (Design's inspector had `substep` mode as an option; deferred). Sub-step line rendering ready on LogStepHeader (accepts `subStep` + `subStepAxis` props) for when substep mode ships. Also affects `/log/edit/[id]` since edit reuses the same wizard component. Type-check clean. |
| T33a | Discover results V1 — Search / Type / Keyword | HIGH | DONE | 2026-07-13. Design's results-surfaces pack shipped (`docs/handoffs/2026-07-13-discover-results-pages-brief.md` for the brief; mockup zip at `outputs/results-redesign/`). Ships four surfaces to match the Discover home visual bar: (1) `/search` — section order swapped to Slimes → Types → Keywords (impact first), added a shimmer skeleton (three rows, 1.2s cycle) during the 300ms debounce, illustrated ooze-blob empty-query + no-results states (line SVG per anti-AI-art rule, cyan blob + magenta dots + cyan search glyph or X), kept our rotating placeholder. `SearchEmptyPrompt`, `SearchNoResults`, `SearchSkeleton`, `OozeBlob` inlined in `apps/web/app/search/page.tsx`. `shimmer` keyframes added to `globals.css`. (2) `/discover/type/<base_type>` — 186px hero card renders the real per-type photo from `/public/guide/textures/` (fed by new shared `apps/web/lib/base-type-hero.ts` module exporting `BASE_TYPE_HERO_PHOTO`, `BASE_TYPE_HERO_TINT`, `DEFAULT_HERO_TINT`), bottom gradient wash for text legibility, top halo bar in the signature color, Montserrat 900 42px type name tinted signature color with matching text-shadow glow, `N logs in the community` sub. TypeCarousel refactored to import from the same module (dedupes constants). (3) `/discover/keyword/<name>` — cyan Montserrat 900 44px `#tagname` hero title with cyan text-shadow, `N logs tagged with #tagname in the community` sub. Reuses `TypeLogsClient` for the log list. (4) `/discover/keyword` — 2-column grid of 132px keyword tiles with deterministic gradient thumbnails per tag name (galaxy / cloud / cyan / magenta / green / gold / clear / ice), `#tagname` in Montserrat 800 + green log count with glow. Live filter input replaces the prior "search keywords" list view. Skeleton tiles shown while loading. `TypeLogsClient` rebuilt: dropped Most Liked (deferred T33d), added a subtype chip row (cyan pills, filters visible logs), sort tabs Newest / Top Rated as green→cyan gradient pills for active / muted glass for inactive (with 160ms ease + press squish), 172px cover log cards with a top-right rating pill (cyan-outlined chip with star + score, backdrop blur), name (Montserrat 700 19px) / brand (magenta) / footer with avatar + magenta @username + muted date over a subtle border-top divider. Empty state uses "Be the first to log a {Butter slime} →" green→cyan CTA with per-type accent tint woven in via the `emptyLabel` + `emptyAccent` props. Related-keywords row (T33e) and Most Liked sort (T33d) intentionally deferred. Type-check clean. |
| T33d | Most Liked sort for /discover/type + /keyword | MEDIUM | DEFERRED | 2026-07-13. Design's mockup for the type and keyword result pages includes a `Newest / Top Rated / Most Liked` sort toggle. We don't have a likes table on `collection_logs` today — implementing Most Liked requires (a) a `log_likes` table with `user_id + log_id + created_at`, (b) a like button on log cards + `/api/likes/toggle` route, (c) a `like_count` trigger updating `collection_logs`, and (d) an index on `like_count` for the sort. For V1 of the results surfaces we ship Newest / Top Rated only. Pick this back up alongside a broader engagement pass. |
| T33e | Related keywords row on /discover/keyword/&lt;name&gt; | LOW | DEFERRED | 2026-07-13. Design mocked a related-keywords pill row on the keyword result page (`#cosmic 88`, `#stardust 52`, etc.) showing tags that co-occur with the current one. Requires a co-occurrence aggregation: `for logs tagged with X, count all other tags in log_tags, return top N by frequency`. Doable via `log_tags` join, but (a) pre-launch we don't have enough data to make it meaningful, (b) at scale it needs a materialized view (`tag_cooccurrence(tag_a, tag_b, count)`) refreshed hourly, otherwise it's an unbounded fan-out on every keyword page view. Cost-tracker entry will follow when we build. Ship without the row in V1. |
| T125 | Slime aging / activator reminders | HIGH | READY | 2026-07-14. Utility feature that turns SlimeLog from a review app into the app power users like Jenn (hundreds of slimes) can't live without. Idea: slime dries out over time and needs periodic activator + kneading. Right now Jenn's slimes just sit until she remembers a specific one; hundreds sit past their optimal window. This ticket = SlimeLog remembers for you. Build spec: (1) New columns on `collection_logs` — `last_activated_at timestamptz` (default = `created_at`), `activation_interval_days int NULL` (per-log override; when null, defaults are derived from the log's `base_type`). Optional column `activation_reminder_muted bool default false` for logs the user wants to opt out of. (2) Per-user setting on `profiles` — `enable_aging_reminders bool default true`, `aging_reminder_hour smallint default 10` (which hour to fire the daily notification digest, user's TZ). (3) Default interval per base type (`lib/aging-defaults.ts`) — butter 30d, cloud 21d, clear 14d, floam 45d, thick/glossy 21d, jelly 14d, snow-fizz 30d, avalanche 21d, magnetic 60d, glass 21d, air-dry 7d (dries fast once opened), etc. All base types get a sane default; Jenn to sanity-check the table pre-launch. (4) New notification enum value `slime_needs_activation` — one row per stale slime with a short deep-link to the log detail. (5) Nightly background job (Supabase Edge Function on cron OR Postgres pg_cron scan) runs `bootstrap_aging_notifications()`: `SELECT id, user_id FROM collection_logs WHERE last_activated_at + (activation_interval_days || ' days')::interval < now() AND NOT activation_reminder_muted AND NOT EXISTS (SELECT 1 FROM notifications WHERE type = 'slime_needs_activation' AND resource_id = collection_logs.id AND created_at > now() - interval '7 days')` — dedupes so we don't spam the same log every night. Fires only for users with `enable_aging_reminders = true`, respects `aging_reminder_hour`. (6) New route `/collection/aging` — user's collection sorted by "most neglected first" (`(now() - last_activated_at) / (activation_interval_days || 'default days') DESC` scoring), with an "Activate all" batch button and per-row "Activate" quick-action that stamps `last_activated_at = now()` without opening the full edit flow. Overdue slimes get a magenta accent, coming-due (within 3 days) get amber, healthy get muted. (7) Optional weekly email digest: "You have N slimes past due for activator" with the top 5 most overdue. Off by default; opt-in via settings. (8) Log wizard extension — Edit page adds an "Activator interval" field under Details (defaults from base type; user can override), plus an "Activated today" checkbox that stamps `last_activated_at = now()`. (9) Log detail page — small "N days since activator" line with an "Activate" button. Turns green when freshly activated, cyan when in window, amber approaching, magenta overdue. Anti-shame framing: never call slimes "dead" or "old"; use "needs attention" / "past due" language. (10) Discover surface later: a "Slime care tips" row that ties into `/guide` when overdue slimes are detected — up-sell path to /guide activator + care sections. Filed cost tracker entry needed at build time: the nightly scan is O(n) collection_logs but capped by dedupe; expected ~1 query per DAU per week. |
| T120 | Comments on collection_logs | HIGH | READY | 2026-07-14. Pairs with T33d (Most Liked sort) — the engagement layer we punted on when shipping the results-surface redesign. Build spec: (1) New table `public.log_comments (id uuid pk, log_id references collection_logs, author_id references profiles, body text, created_at, updated_at, parent_id uuid NULL for one-level thread replies)`. RLS: anon can read comments on public logs, authenticated can insert their own, author can update/delete their own. Comment body runs through `lib/moderation.ts` (see moderation rules in CLAUDE.md). Add `comment_count int default 0` denorm on `collection_logs` + AFTER INSERT/DELETE trigger to keep it fresh. (2) `CommentsThread` component under `apps/web/components/log/` — top-level comment composer + list of comments with 44px author avatar, `@username` (magenta), body, relative time, one-level reply thread, per-comment delete for own comments. Reply button opens an inline composer indented under the parent. (3) Server actions `submitComment(logId, body, parentId?)` returning `{ ok: true, comment } | { ok: false, error }` (result union per server-action rule, no throws). (4) Notification hooks: comment on a log fires `log_comment` notification to the log author; reply fires `comment_reply` to the parent comment's author. Both types added to notifications enum (T29b lists this as future-ready). (5) Rate limit: 1 comment per 15 sec per user per log at the DB level (unique partial index or trigger). Prevents spam. (6) Cost tracker entry filed. Deep dependencies on T33d (likes) — build these two together makes sense once we're back on engagement work post-launch. |
| T121 | Push notifications for drops (brand followers) | HIGH | READY | 2026-07-14. Item #4 in the tracker already lists "Push notifications" as READY at a high level; this is the concrete first-use-case spec. When a followed brand publishes a drop that transitions to `live` OR is `announced` and hits its `drop_at` timestamp, every user in `brand_follows` where `brand_id = drop.brand_id AND notifications_muted = false` gets a push. Requires: (1) Capacitor packaging (T23 blocked on RevenueCat chain) OR web push via VAPID for the PWA build in the meantime. (2) `notifications` table already carries `type = 'drop_announced' | 'drop_live'` (see T29b future-ready list) — INSERT rows on drop status transitions via a Postgres AFTER UPDATE trigger, then a worker fans them out to APNs/FCM. (3) Per-brand notification opt-out toggle on the brand detail page (default on when user follows). (4) Daily digest fallback ("3 drops from brands you follow today") for users with per-drop pushes muted. Pairs with T39 tag-flow audit — brand-side notifications when their slime is logged is the other half of the funnel (T122). |
| T122 | Brand-side notification center | HIGH | READY | 2026-07-14. Brand owners need real-time signal when a user logs one of their slimes, tags them in a suggestion, follows them, or reshares. Right now they'd have to poll their dashboard. Build: (1) Extend notifications enum with `brand_log_received`, `brand_follower_gained`, `brand_slime_rated_low` (heads-up when a log rates them <2 avg, so the brand can respond gracefully), `brand_slime_rated_high` (celebration/reshare fuel), `brand_reshare_hit` (someone shared the brand from within SlimeLog). (2) `brand_notifications` scoping — `notifications.recipient_id` is a `profiles.id` (owner) but the notification carries `brand_id` so multiple co-owners of a brand all get the ping. When brand_dashboard lists notifications, filter by ownership. (3) NotificationRow already handles multiple types; add renderers for the new ones. (4) Fires the tag-flow flywheel spec'd in T39 — brand sees the log within 30 seconds of it being written, hits "Reply" (T123), user's log gets a magenta "Brand replied" chip. |
| T123 | Reply-to-log (brand → user) | HIGH | READY | 2026-07-14. Brand owners can reply directly to a log written about one of their slimes. This is a specific type of comment (T120) with brand-account attribution instead of user attribution, and a signature magenta treatment on the log page so the reply is instantly recognizable. Build: (1) Use the `log_comments` table from T120 but add optional `brand_id uuid references brands` — when set, the row renders as a brand reply with the brand's logo + name + magenta accent border instead of the author's avatar. RLS: brand owners can INSERT with `brand_id = <owned brand>` where `log.brand_id = <same brand>` (only reply to logs about your own slimes). (2) On the log detail page, brand replies pin to the top of the comments thread and get "Brand response" pill. (3) Fires `brand_replied_to_log` notification to the log author. (4) One-per-log-per-brand rate limit to prevent brand spam. Depends on T120. |
| T124 | Brand posts / announcements feed | MEDIUM | READY | 2026-07-14. Brands need a first-party channel to publish updates that AREN'T drops — restock timing, ingredient changes, giveaway announcements, community shoutouts. Right now they cross-post to IG and hope followers see it. Build: (1) `brand_posts` table (`id`, `brand_id`, `body text`, `image_url text NULL`, `created_at`, `pinned bool`). (2) Rendered on brand detail page above Upcoming Drops (glass card list, 3 most recent with "See all →" for archive). (3) Fires `brand_post_published` notification to followers (respects mute toggle from T121). (4) Also surfaces in the main `/` feed for followers as a distinct card type. (5) Pinned post gets a magenta pin icon and stays at top of the brand's post list. Moderation via `lib/moderation.ts`. Brand-authored copy IS moderated (unlike admin-authored per CLAUDE.md) since brands aren't fully trusted admin roles. |
| T126 | Deep-link log wizard from drop card | MEDIUM | READY | 2026-07-14. When a user opens a drop they bought and taps "Log this drop" on the drop detail card, the log wizard should pre-fill: (1) `brand_id` from the drop's brand, (2) `slime_name` from the drop's `name`, (3) `image_url` from the drop's `cover_image_url` if the user hasn't uploaded their own photo yet. (4) `base_type` from the drop's metadata if we start capturing that on the drop (probable T133 companion). Pass via `?fromDrop=<id>` query param, log page reads it in the initial useEffect, hydrates form state before render. Cuts log-from-drop friction from ~30 sec to ~5 sec. Should measurably lift log rate on drop days. |
| T127 | Reactions on logs (emoji, not full comment) | MEDIUM | READY | 2026-07-14. Lightweight engagement layer that's less friction than a comment but more expressive than a like. Fixed reaction set (5): 👍 (like), ❤️ (love), 🔥 (fire), 🎯 (nailed it), 🙌 (celebrate). Emoji use is fine in reaction UI (CLAUDE.md rule: "no emoji in code paths unless the user explicitly asks. Existing emoji stays." — reactions are one of the rare paths where emoji is core UX, similar to Slack). Table: `log_reactions (id, log_id, user_id, reaction_type, created_at)` with `unique(log_id, user_id, reaction_type)` so a user can add multiple different reactions but not double up on the same one. Batch-fetch reaction counts server-side on log lists; render as pill cluster below the rating pill. Filed as MEDIUM — a "comments feel too much, likes feel too little" middle ground. Ships alongside T120 comments makes sense. |
| T128 | Wishlist / want-list notifications | MEDIUM | READY | 2026-07-14. Users add slimes they want to a wishlist. When any of these events happen the user gets a push: (1) the wanted slime shows up in a brand's drop, (2) another user resells the exact slime on the marketplace (post-launch), (3) the brand restocks (if brand publishes restock via T124). Build: `wishlist_items (id, user_id, slime_id NULL, brand_id NULL, free_text NULL, created_at)` — flexible enough to want a slime we don't have in the catalog yet. Cross-reference against `drops` on drop_at approach + against `slimes` on new INSERTs. Fires `wishlist_match` notification. Also surfaces a "You have 3 slimes on your wishlist" nudge on the profile page. |
| T129 | Slime trade/swap board | LOW | READY | 2026-07-14. Peer-to-peer swap board (not the paid marketplace — that's Stripe Connect per CLAUDE.md). Users list slimes they'd trade for other slimes ("Have Butter Bear, want Kai's Cloud" style). No payments, just DMs and shipping between users. Build: `swap_listings` table, DM system (would need a full messaging build), rate limits. Low priority — the paid marketplace probably subsumes the swap use case once shipped, and building a full DM system is a large lift for a small feature. Note here so we don't forget the discussion happened. |
| T130 | Live-viewer count on drops | LOW | READY | 2026-07-14. When a drop status transitions to `live` and users are actively on the drop detail page, show a "N people watching now" counter for social pressure / urgency. Realtime via Supabase Realtime presence channels, ephemeral (never persisted). Only shows above threshold (e.g. 5+ viewers) to avoid "1 person watching" being embarrassing on early drops. Low priority — cute nice-to-have, not a moat feature. |
| T131 | Batch tracking (batch numbers on slimes) | LOW | READY | 2026-07-14. Some slime makers batch-number their slimes ("Batch 47 of Butter Bear"). Power users like Jenn (hundreds of slimes) sometimes track batch-to-batch consistency: is butter batch 43 more stretchy than 47? Add optional `batch_number text NULL` on `collection_logs`. On brand detail page, aggregate ratings can filter by batch — surfaces "Batch 47 rates 4.3, batch 51 rates 3.1" trends that brands can act on. Low priority, deep-collector feature; ships once we have serial loggers. |
| T132 | Compare mode for two slimes | LOW | READY | 2026-07-14. Side-by-side comparison view for two collection_logs. Both photos, both rating spider charts (or bar rows), tag intersect + differences highlighted, "Winner on Texture" per-axis. Deep-link `/compare?a=<log_a>&b=<log_b>`. Powered from a "Compare" button on log detail + a multi-select mode on `/my-shelf`. Low priority — nerd feature that power users LOVE but casuals will never use. Good candidate for a "Pro" tier feature (T22 chain). |
| T133 | Brand banner + cover image upload UI | MEDIUM | READY | 2026-07-14. `brands.banner_url` is already used on the brand detail page (T38). But there's no UI in `/brand-dashboard/<slug>` for a brand owner to upload / crop / preview one. Build: upload UI in the dashboard settings page, Supabase Storage bucket for brand banners with per-brand RLS (owner writes, anon reads), 2000×600 recommended aspect, JPG/PNG/WebP, 5MB cap, client-side crop UI matching the 200px-tall display on the brand page. Ships alongside T137 brand dashboard redesign. |
| T134 | Brand `tubs_available` inventory editor UI | MEDIUM | READY | 2026-07-14. `drops.tubs_available` shipped in migration `20260713000073` (T38). No dashboard UI exists for a brand owner to set/update the number. Build: field on the drop create/edit form in `DropsSplitPanel` (currently `apps/web/app/brand-dashboard/[slug]/drops/page.tsx` selects the column but nothing edits it). Number input with clear affordance (Design's note: this is a snapshot, not a live count — copy on the field should say "How many tubs are currently available"). Optional "Sold out" toggle that sets `tubs_available = 0`. |
| T135 | Brand onboarding checklist | MEDIUM | READY | 2026-07-14. When a brand claims their profile via `ClaimBrandButton` (or admin approves a brand suggestion), lead them through a 4-step checklist: (1) Upload logo + banner (T133), (2) Fill bio + location + restocks schedule, (3) Publish first drop, (4) Reply to a log (T123 required). Progress persists via `brands.onboarding_completed_at`. Dismissible but the count-of-steps-remaining pill lives in the dashboard header until done. Analogous to user onboarding T31. |
| T136 | User onboarding refresh (post-Discover-redesign) | MEDIUM | READY | 2026-07-14. Item #31 (Onboarding) shipped in July but pre-dated the Discover / brands / log-wizard redesigns. Screenshots + copy in the 4-screen onboarding modal now reference visuals we've replaced. Rework: refresh the illustration frames to match the new visual family, update the "Rate what matters" screen to show the current 6-axis rating scale, add a screen for "Follow brands you love" between "Log your first slime" and "Invite friends" (adds the auto-follow-seed context from T36 in a way that also markets brand-following). |
| T137 | Brand dashboard visual redesign | HIGH | READY | 2026-07-14. Only major user-facing surface not yet run through the Design agent's redesign track. Current dashboard at `/brand-dashboard/<slug>` is functional but visually a step behind Discover / brands / log-wizard / brand-detail. Handoff brief needs writing — surfaces to cover: dashboard home (KPIs, recent activity, quick actions), Drops split-panel, Notifications page (once T122 ships), Settings (banner/logo upload once T133 ships, tubs_available default once T134 ships), Team (once we have multi-owner). Reuse: `sectionCardStyle` from LogWizardShared, brand-gradients module, magenta pro treatment from brand detail. Deferred flowchart items to flag in brief: how to handle brand-pro visual reward within the owner's own dashboard (subtle vs. visible), how to surface the brand-notifications inbox (bell? left-nav count? separate page?), whether stats show public numbers or owner-only unpublished ones. **2026-07-16 addition (from taxonomy plan Section 5.6 resolution):** include a "featured variants" controller on the brand settings page. Brand owner picks up to 3 subtypes from their available variants and those get promoted to top-level filter chips on their public brand detail page. Requires a new `brand_top_level_variants` table (`brand_id`, `subtype_id`, `display_order smallint`) + a small UI in the settings sub-nav. Motivating case: Poppy Mello uses "Metallic" as a top-level nav item on their own shop (elsewhere Metallic is a Clear subtype). Pattern expected to recur as more brands claim catalogs — better to build inside the dashboard redesign than as one-off infra. |
| T138 | /slimes/[id] detail page visual redesign | MEDIUM | READY | 2026-07-14. Log detail page hasn't been through the redesign track. Currently functional but pre-dates the visual family established across Discover / brands / log wizard. Should get the same treatment: hero photo full-bleed, big Montserrat 900 slime name, brand line with logo, per-axis rating cards (mini version of the wizard's `RatingAxisCard`), notes prose, comments (T120) + reactions (T127) + brand reply (T123) once those ship. Draft brief when Design cycle opens up. |
| T139 | /drops/[id] detail page visual redesign | MEDIUM | READY | 2026-07-14. Drop detail page similar situation to T138 — hasn't been redesigned. Needs: hero cover, T-minus pill treatment (already established on brand detail), brand context row, tubs_available line (T134), shop_url CTA, "Log this drop" deep-link (T126), share button, upcoming drops from same brand row. Brief pairs naturally with T137 brand dashboard brief since the brand publishes the drop from the dashboard. |
| T140 | First-touch route audit (waitlist / landing / signup / login / welcome) | HIGH | READY | 2026-07-14. Pre-launch blocker candidate. We've heavily redesigned every logged-in surface but the first-touch routes users hit before they see the redesigned app haven't been audited against the new visual family. Audit: `/waitlist`, `/landing` (if exists), `/signup`, `/login`, `/welcome`, `/forgot-password`, `/reset-password`, `/auth/*` error pages. Each should carry the same Montserrat 900 headline treatment, the cyan/magenta/green palette, the glass card treatment, orbs where appropriate. Anything that still uses old Tailwind defaults or pre-redesign copy gets flagged. Also verify: waitlist signup flow is smooth on mobile (this is the primary growth channel via Jenn's IG), no dead ends after email confirmation, no em-dashes in copy (SlimeLog house rule). Ship before Instagram launch cycle. |
| T141 | Settings pages visual family alignment | LOW | READY | 2026-07-14. `/settings/*` pages are functional but stylistically a step behind the redesigned surfaces. Not user-critical but worth a pass once first-touch routes (T140) and brand dashboard (T137) are done, so the whole app has one visual voice. Low priority relative to launch-blocking items. |
| T142 | Sign in with Apple (SIWA) implementation | HIGH | READY | 2026-07-14. Confirmed required by James — /login + /signup currently offer Continue with Google via Supabase OAuth, and Apple mandates SIWA at equal visual weight anywhere 3rd-party SSO is shown (or the App Store rejects at review). Supabase natively supports SIWA so this is a small build. Steps: (1) create an Apple Services ID + Sign in with Apple key in the Apple Developer Portal, (2) upload the key + set the team ID + services ID in Supabase Auth settings (Auth > Providers > Apple), (3) add "Continue with Apple" button component styled to match the existing "Continue with Google" button (same size, same visual weight, both above/below the email form), (4) callback flow works via the existing /auth/callback PKCE route with no additional code (T33 fix already routes all OAuth callbacks through it), (5) test signup + login + link-existing-account flows in sandbox, (6) test on both web and iOS Capacitor build (Apple requires native SIWA on iOS, browser fallback on web). Also update the onboarding first-touch to show both options equally (T136 refresh already planned for this). ~1 day of build. Small risk: Supabase's SIWA callback returns a temporary email like `abc.def@privaterelay.appleid.com` if the user chose "Hide My Email" — profile creation flow must handle this gracefully (no assumption of email uniqueness matching the actual owner). |
| T143 | App Store metadata pack (icon + screenshots + copy) | HIGH | READY | 2026-07-14. All the App Store Connect listing content Apple requires before submission. Design + copy work, not code. Deliverables: (1) master app icon at 1024x1024 (Xcode auto-generates the 20+ smaller variants from this), keep it geometric per anti-AI-art rule — signature radial with slime name treatment probably. (2) Screenshots for 6.7" iPhone at minimum (1290x2796px) — Apple accepts one device size and scales but 6.7" gives best clarity across the range. Aim for 5 screenshots: Discover, log wizard mid-rating, brand detail, follow feed, /guide. Iterate copy overlays with Design agent. (3) iPad screenshots only if we're targeting iPad in the initial submission (recommend deferring iPad → v1.1 to keep the review surface small). (4) App description (up to 4000 chars, first 3 lines are what's above the fold in App Store). Draft in the SlimeLog voice — no em-dashes per house rule. (5) Keywords field (100 chars, comma-separated, no spaces) — slime, asmr, satisfying, rating, review, collection, tracker, community, brand, etc. Keyword strategy needs a research pass. (6) Support URL + Marketing URL + Privacy Policy URL — support goes to slimelog.com/support (or wherever the support form lives), marketing to slimelog.com, privacy to slimelog.com/privacy. (7) Promotional text (170 chars, updatable without a new build). (8) What's New (release notes for v1.0.0). 2 to 3 days of coordinated design + copy work. |
| T144 | Privacy Policy App Store delta pass | HIGH | READY | 2026-07-14. James's existing privacy policy is fine for web but needs a delta pass for App Store submission. Apple's privacy nutrition label form asks disclosure about specific data categories that MUST be reflected in the policy or the app is rejected at review (Apple cross-checks). Audit the current policy against these Apple categories: (a) auth identifiers (email, user ID) — required disclosure, (b) device identifiers (Apple IDFV, install ID, session ID), (c) user content (logs, ratings, comments, photos), (d) contact info (email + optional social handles), (e) usage data (feature interactions, session length, in-app browsing), (f) diagnostics (crash logs, performance metrics), (g) purchases (via Apple IAP + RevenueCat + Stripe web), (h) location (if we're using geo for restocks or drops — verify). For each present-in-code data type, the policy must state: what's collected, why, whether it's linked to user identity, whether it's used for tracking. 1 to 2 hours of audit + append. If gaps found, publish updated policy before nutrition label fill (T145). |
| T145 | Privacy nutrition label form fill (App Store Connect) | HIGH | READY | 2026-07-14. Apple's mandatory data-collection disclosure form in App Store Connect. Filled once, updated on every submission if data collection changes. Must match code truth exactly — Apple compares your disclosures against static analysis of the binary AND against your privacy policy (T144). Any mismatch = review rejection. Steps: (1) walk through each of Apple's 14 data categories, mark used/not-used based on what SlimeLog actually collects, (2) for each used category flag "linked to user identity" (all our data is because we have accounts), "used for tracking" (only if we add ads/attribution — currently NO), "used for third-party advertising" (NO for now), (3) declare purposes (analytics, app functionality, product personalization, developer's advertising / marketing). Requires T144 done first for policy alignment. 1 hour of clicking + double-checking against `docs/error-tracker.md` for any data-collection paths we might have forgotten. |
| T146 | Age rating questionnaire completion | HIGH | READY | 2026-07-14. Apple's age rating form in App Store Connect. 15 to 20 questions about mature content, violence, sexual content, gambling, etc. For SlimeLog: user-generated content + unmoderated interactions land us at 12+ minimum (Apple's baseline for any UGC app). Confirm no other bumps: no gambling, no explicit content, no unrestricted web access, no medical/treatment info, no drug references. Answer honestly — over-declaring is fine, under-declaring gets you rejected. 15 minutes. |
| T147 | Support email + Resend auto-reply | HIGH | READY | 2026-07-14. Apple requires a monitored support email URL in the App Store listing. Set up support@slimelog.com (or whichever domain) with either a real human triage inbox or a Resend auto-reply. Since T41 has Resend already consolidated, wire a simple auto-reply route: incoming to support@ → reply within 5 min with a canned "we got your message, we respond within 24 hours" note in the SlimeLog voice. Add a `/support` page (basic contact form → sends to support@ via Resend + logs to a Supabase `support_tickets` table for triage) so both channels work. Apple reviewers WILL email this address during review — auto-reply plus human follow-up within 24h keeps review clean. ~30 min setup + 1 hour if we also want the support page. |
| T148 | App Review Information pack (demo credentials + reviewer notes) | HIGH | READY | 2026-07-14. Apple's App Review team needs demo account credentials + notes on any non-obvious flows so they can actually test the app in review. Deliverables: (1) Demo user account (`apple.review@slimelog.com` or similar) pre-seeded with a shelf of logged slimes, a few follows, and — critically — an active Pro subscription attached via RevenueCat sandbox so reviewers can test paywalled features without buying anything. (2) Demo brand account (`apple.review.brand@slimelog.com`) that owns a claimed brand with drops + community logs, so reviewers can test the brand-owner surface too. (3) Notes text (up to 4000 chars) explaining the log wizard's rating slider (touch behavior specifically since Jenn's slider-touch bug was tricky), the sharing flow, how to test IAP purchase + restore, why we require signup (community app), that we do NOT show ads in v1, that all UGC goes through moderation. (4) If the review flags anything, respond within 24h — App Review responds by email to your Apple ID's email address. 30 min to prep, plus watching the inbox during review. |
| T149 | RevenueCat ↔ Stripe cross-platform entitlement verification | HIGH | MOSTLY DONE | 2026-07-14. **Monthly path verified end-to-end.** Full walkthrough completed at workstation. Shipped: (a) code change to `apps/web/app/api/stripe/checkout/route.ts` that writes `app_user_id: user.id` in Checkout Session metadata + Subscription metadata (RevCat's detection reads Session + Subscription metadata, not Customer metadata — Customer metadata is where our legacy `supabase_user_id` still lives and is kept for backward compat with our webhook handler). (b) Created dedicated Stripe webhook endpoint (Workbench UI, now under Event Destinations) pointing at RevCat's Stripe Webhook Endpoint URL with EXACTLY 6 events: `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `charge.refunded`, `invoice.updated`, `checkout.session.completed`. Any extra events cause RevCat to 400 and Stripe to eventually disable the webhook. (c) Signing Secret copied from Stripe into RevCat's Stripe Webhook Secret field. (d) RevCat App User ID detection method flipped from `Use anonymous App User ID` to `Read App User ID from a Stripe metadata field` with key `app_user_id`. (e) Imported SlimeLog Pro Monthly product from Stripe into RevCat (SlimeLog Pro Yearly import currently BLOCKED, RevCat support ticket open, see T150 backfill dependency). Brand Pro deliberately NOT imported — web-only forever, no cross-platform unification value, and its subscription metadata carries `brand_id` not `app_user_id`. (f) Created `pro` entitlement (identifier `pro`, display name "SlimeLog Pro tier"). (g) Attached SlimeLog Pro Monthly to `pro` entitlement. (h) Created `default` offering with `$rc_monthly` package pointing at SlimeLog Pro Monthly. Marked offering as Current. **Live sanity check verified:** fresh test signup on slimelog.com bought Pro Monthly $2.99 via existing Stripe Checkout. Within ~10 seconds RevCat showed the customer record with App User ID matching the Supabase profile UUID, subscription Active, `pro` entitlement Active with 1-month expiry, revenue tracked at $2.99. Test purchase refunded via Stripe. **Remaining before this ticket fully closes:** T149a — get RevCat support to resolve SlimeLog Pro Yearly import failure (their support UI just said "Import complete. 1 product failed" with no detail; ticket opened 2026-07-14). Once yearly imports, attach it to `pro` entitlement + add `$rc_annual` package to `default` offering + run same sanity check flow with a yearly purchase. Also T150 (backfill of any pre-existing Stripe subs that predate the app_user_id metadata write) will need to run before iOS launch. |
| T150 | Backfill existing Stripe Pro subs into RevenueCat | MEDIUM | READY | 2026-07-14. Split off from T149 during the walkthrough. Existing Stripe Pro subscribers (created before the T149 code change shipped) don't have `app_user_id` in their subscription metadata, so RevCat won't sync them via server notifications — those subscribers would show as free-tier on iOS at first launch. Two viable approaches: (a) **Backfill via RevCat receipts endpoint** — script iterates over Stripe subs in `active`/`trialing` status, POSTs each to `POST https://api.revenuecat.com/v1/receipts` with `X-Platform: stripe`, header `Authorization: Bearer <RevCat Stripe app public API key>`, body `{ app_user_id: <supabase profile id from customer.metadata.supabase_user_id>, fetch_token: <subscription id sub_...> }`. Cleanest, one-time script. (b) **Update Stripe subscription metadata in place** — script iterates over active subs, calls Stripe's subscription update API to add `metadata: { app_user_id: <profile id> }`, which triggers RevCat's server notification handler to pick them up naturally. Simpler to reason about but noisier (fires webhook events). Recommend approach (a). Pre-launch state has ~0 to a handful of Pro subs so this is not urgent, but must run before iOS launch or those users lose Pro on the app. Filed after T149 confirms the notification pipeline works. |
| T157 | Brand catalog dedupe + expand + trigger fix | HIGH | DONE | 2026-07-16. Two-part cleanup ahead of paid promo. **Data cleanup:** merged 9 duplicate brand pairs (Peachybbies, Bleu Slimes, Sliimey Honey, Snoop Slimes, Corn With Slime, Dream Glow Slimes, GT Creation Slime, Pink Sugar Slimey 3-way, Sloomoo Institute, Slime Community MGA) + KSC/Kawaii Slime Company merge = 12 delete-side rows collapsed into the canonical winners. Renamed 4 mis-slugged brands (Cosmic Slimez → Cosmic Slime, Minty Fresh Slimes → Minty Slimes, King Slime Company (BR) → King Slime Brasil, Royal Slime Shop CA → Royal Slime). Disambiguated Colour Slime AU/US names (both were literally "Colour Slime"). Deleted 2 misclassified rows (Holly Laing Slimes = merch site not shop; GuiGui Slime = Moose Toys corporate page). Inserted 18 Korean creator brands sourced from SeoulGAGE curator (332, ABOUTTIME, BBIYA, GINA, HO.C, MAGMA, MOA, MONGLEFACTORY, ONI, PAINKILLER, PASTELLO, SG Slime, SINGIBAKERY, SLRK, SWAMPYLAND, WAAK, YOM, YYOUNG) — Korea flagged as high-priority expansion market post-launch. Added normalized-name unique index (`brands_normalized_name_key`) preventing future dupes like "Snoop Slimes" and "Snoopslimes" from both landing. **Trigger fix:** Broadened `slimes_protect_attribution` (audit HP-11) from `current_user = 'service_role'` bypass to `current_user NOT IN ('authenticated', 'anon')` bypass, matching the mig 59 pattern already applied to profiles + brands protective triggers. First attempt of the migration failed silently because the narrow bypass blocked the FK reassignment UPDATE — Postgres's atomic transaction rolled it back cleanly. See docs/error-tracker.md 2026-07-16 "Protective trigger silently reverts migration writes" for full RCA + prevention patterns. **Verified counts:** 266 original → 270 after (12 merged + 2 deleted + 18 Korean added = net +4). Every duplicate winner retained verified status via OR flag transfer. Files: `supabase/migrations/20260716000076_brand_catalog_dedupe_and_expand.sql`, `docs/error-tracker.md`. Follow-ups: Snoop Crunch Slime Shop preserved (Jenn didn't call out) but may be another product-line variant to dedupe later; Colour Slime AU/US kept as distinct rows per Jenn's default (region variants). |
| T156 | Featured shop admin selector (or dynamic top-log-count) | MEDIUM | READY | 2026-07-16. Per Jenn's brand catalog audit 2026-07-16. Currently the /brands page Featured shop slot renders Align Slimes which has 0 logs and 0 followers — hurts credibility (empty-featured is worse than no-featured). Two viable fixes: (a) admin-manual — add a `featured_at timestamptz NULL` column on `brands`, admin picks which brand to feature via /admin, /brands page selects `WHERE featured_at IS NOT NULL ORDER BY featured_at DESC LIMIT 1`. Full editorial control, matches Jenn's operator instincts. (b) auto-pick — SELECT the brand with the highest log count in the last 14 days, tie-broken by verified status + follower count. Ships without admin work but can pick something Jenn wouldn't. Recommend (a) — Jenn's slime-brand taste is a competitive advantage, editorial selection uses it. Small migration + 30 min of admin UI + 30 min of /brands page query change. |
| T155 | Featured shop card rendering bug ("No ratings 01 logs") | MEDIUM | READY | 2026-07-16. Per Jenn's brand catalog audit 2026-07-16. `FeaturedBrandCard.tsx` renders the featured shop's rating strip as "No ratings 01 logs" — should be "No ratings · 1 log" or similar. Two things wrong: (1) missing the separator dot/space between the two stats (renders adjacent as a run-on), (2) log count shows `01` instead of `1` (numeric formatting bug, probably a leading-zero pad from a template literal or a String.padStart in the wrong place). Straightforward code fix, probably 15 min in `apps/web/components/FeaturedBrandCard.tsx`. Pair with T156 so we're not touching the same file twice. |
| T154 | Restock schedules for top 20 verified shops | LOW | READY | 2026-07-16. Per Jenn's brand catalog audit 2026-07-16. Every community brand currently shows "No restock schedule" on their brand detail page. Drop notifications are the killer feature (T121 push notifications, T124 brand announcements) but the data behind them is empty. Not code work — Jenn (or an admin) manually enters restock schedules for the top 20 verified shops via /brand-dashboard/<slug>. Or add a `restock_schedule text NULL` column + free-text edit affordance in admin. Half a day of data entry once T133 brand banner upload UI ships, since the brand dashboard's settings surface is being redesigned around that same window. Realistic to punt until T137 brand dashboard redesign so it's not touched twice. Pre-launch nice-to-have not a blocker. |
| T153 | Seed 50-100 brand ratings before launch | HIGH | READY | 2026-07-16. Per Jenn's brand catalog audit 2026-07-16. All 263 shops currently show "No ratings yet · 0 logs" on the /brands page. Empty pages kill credibility with both collectors AND the shops we're pitching to onboard. Jenn should personally log 50-100 slimes across her top brands before we point paid promo at the app. This is not code work — action item for Jenn/James manual entry via the standard log wizard. Prioritize by expected traffic: verified brands first, then top 10 community brands by Instagram following. Blocks the "big push" moment even though it doesn't block a deploy. Rough time budget: 3-5 hours across a couple sessions. |
| T152 | Taxonomy rework Phase 1 — clay removal | HIGH | DONE | 2026-07-16. First phase of the 8-phase taxonomy rework per `docs/handoffs/2026-07-15-taxonomy-rework-plan.md`. Pre-check verified zero rows tied to `clay` across `slimes`, `collection_logs`, and `subtypes` — so this is pure enum surgery with no data touched, no user-visible impact. **Migration** `20260716000075_remove_clay_base_type.sql`: defensive backfill (`clay` → `butter`, no-op given the pre-check but stays as a safety net), sanity DO block to raise if any clay rows remain, drop-and-recreate of the 3 views that reference `base_type` (`top_rated_slimes`, `brand_top_slimes`, `user_collection_summary`) — identical pattern to migration 20260509000037 which established this dance. Enum surgery via rename-old → create-new-19-values → ALTER TABLE ... USING (::text::new_enum) on all three tables → DROP TYPE _old. **Code sweep** (11 files): dropped `clay` from `apps/web/lib/types.ts` (SlimeBaseType union + LABELS + COLORS), `lib/base-type-hero.ts` (BASE_TYPE_HERO_PHOTO + BASE_TYPE_HERO_TINT), `app/api/wishlist/route.ts` (BASE_TYPES validator array), `components/FeedCard.tsx` (style map), `components/feed/FeedCardCompact.tsx` (label map), `components/collection/CollectionCard.tsx` (chip map), `components/collection/TasteInsights.tsx` + `SpiralView.tsx` + `TimelineView.tsx` + `CollectionSummaryChart.tsx` (chart color maps), `app/slimes/[id]/opengraph-image.tsx` (OG background color map). **Guide** `app/guide/content.ts`: deleted the entire `slug: "clay"` base type entry (Palmetto Clay example, "Smooth, soft, and endlessly moldable" tagline, gold gradient — gone), removed `"Clay"` from the Butter entry's `variantsAndRelated` list. Kept: Slay entry copy referencing "'slight' and 'clay'" (clay as INGREDIENT, not a base type), Butter/Cloud Cream definitions mentioning "air-dry clay" as an ingredient, Air Dry Clay add-ins section. Hybrid hero photo filename `hybrid-bingsu-jelly-clay-ky.webp` kept — filename is the file's identifier, not a base_type reference. **Grep-verify** confirmed zero remaining `clay:` object keys and only 3 acceptable string references (the plan's Slay ingredient copy + two explanatory removal comments). Type-check clean. **Push order matters:** run `npx supabase db push` BEFORE Vercel picks up the code push, because the code no longer accepts `clay` as a value but the enum still would in the DB. If code deploys first and someone tries to log a clay slime in the deploy-vs-migration window, it'd 500. Realistically zero users are trying that during a routine push but worth calling out. Files: `supabase/migrations/20260716000075_remove_clay_base_type.sql`, plus 12 TS/TSX files changed. |
| T151 | Waitlist attribution capture (heard_from + UTM) | HIGH | DONE | 2026-07-15. Side quest ahead of paid promo + giveaway launch. Backend + frontend + admin all shipped end-to-end in one session. **Backend:** migration `20260715000074_waitlist_source_tracking.sql` adds `heard_from` (self-report picker value, allowlist-normalized in the API) + 5 UTM columns (`utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `utm_term`) to `public.waitlist`. All nullable, partial indexes on `heard_from` + `utm_campaign` for segmentation dashboards. No CHECK constraint on heard_from so picker options evolve without a migration; frontend + API share an allowlist (`instagram`, `tiktok`, `youtube`, `friend_or_family`, `giveaway`, `search`, `other`) with `other:<free text>` sentinel support (80 char cap). UTMs pass-through capped at 200 chars. `/api/waitlist` accepts + normalizes all 6 fields; `lib/brevo.ts` propagates `HEARD_FROM` to Brevo as a contact attribute for list segmentation. Requires Brevo dashboard follow-up: add `HEARD_FROM` TEXT attribute (Jennifer completed 2026-07-15). **Frontend:** `apps/web/app/waitlist/page.tsx` rewritten — replaced Design agent's pill picker with a styled native `<select>` dropdown per Jenn's UX feedback (chips visually competed with primary CTA + didn't scale as promo mix grows). Auto-captures UTM params from URL via `useSearchParams` wrapped in Suspense; silent to the user, POST'd alongside self-reported heard_from. Also fixed the "card layering feels dark" issue Jenn flagged — added per-color box-shadow glow on FloatingPills so the halo bleeds through the card blur (matches Design's mockup pattern), bumped default pill opacity 0.85 → 0.92, reduced card alpha 0.35 → 0.28, added inset box-shadow to preserve card depth without darkening. **Admin:** `apps/web/app/admin/waitlist/page.tsx` extended — pulls all 6 new columns via a defensive-fallback query (destructures `{ data, error }` per CLAUDE.md rule; falls back to legacy columns if the migration hasn't landed in the current environment; renders an amber "Migration pending" banner when the fallback fires). Two new pills (`HeardFromPill` magenta with `other:<free text>` handling; `UtmCampaignPill` green), new "With Attribution" gold stat card as a health metric, two new table columns (Heard From + Campaign). `ExportCSV` extended with all 6 fields for spreadsheet analysis. **Diagnostic story:** first test batch landed with heard_from=null. Turned out to be a git-add-missed-a-file bug (`lib/brevo.ts` changes were on disk but never committed, so deployed API called a function with an arg its type didn't accept, Vercel build failed silently on the intermediate diagnostic push). See `docs/error-tracker.md` 2026-07-15 "Multi-file feature commit stranded one file uncommitted" for the full RCA + three prevention patterns. Follow-ups: none critical. Diagnostic logs stripped from prod code after verification; if paid promo attribution comes in noisy or with wrong values we may want to add a "campaign name normalizer" to collapse UTM variants but wait for real data first. |
| 34 | Profile completeness nudge | MEDIUM | READY | Prompt to add avatar, bio. |
| 44 | Brand dashboard subscription card | MEDIUM | READY | Show current tier + upgrade CTA. |
| 45 | Public profile Brands tried tile | MEDIUM | READY | Scroll-through list of brands in collection. |
| 47 | User-submitted brand additions | MEDIUM | READY | 2026-07-10. Users need a way to submit a slime brand that doesn't exist in the catalog. Likely a "Suggest a brand" button/CTA on /brands (the discovery list) that opens a small form: brand name, website/socials, why it should be added. Submissions go to a pending queue for admin review before appearing publicly (avoids spam / duplicate brand pollution). Ties into brand claims flow — once approved, the original brand submitter could be tagged as the discovery source. |
| T35 | Slime of the Week/Month/Year | MEDIUM | DEFERRED | Major engagement feature. Defer post-launch but design data structures now. |
| T36 | Wizard step-1 escape behavior | MEDIUM | WON'T DO | 2026-07-11: decided against adding a back button on /log step 0. Users navigate away via the nav menu. |
| T47 | Email editing during claim flow step 1 | MEDIUM | DONE | Verified 2026-07-11: ClaimBrandForm.tsx line 125 passes claim_id when present, submit route (line 160-208) PATCHes the existing row via .update().eq("id", claim_id) instead of inserting. Ships user back through step 1 with the corrected email without losing the claim. |
| T51 | Standardized rejection reason dropdown | MEDIUM | DONE | Verified 2026-07-11: ClaimReviewActions.tsx uses RejectionReasonCode from lib/types.ts with 5 standardized codes (documentation_insufficient, email_unverified, role_unconfirmed, suspected_fraud, different_owner_indicated) + "other" (which requires >=10 char free-text context). |
| T52 | Hide consumer bottom nav on admin pages | MEDIUM | DONE | Verified 2026-07-11: BottomNavWrapper already short-circuits for `/admin` and `/admin/*` (line 35). Was implemented in bundle C. |
| T99 | Signup page copy polish | MEDIUM | DONE | 2026-07-11: killed the placeholder pill row ("Butter · Cloud · Icee · Clear · Slay · +46 more"). Replaced the subhead "Track, rate, and discover slimes you'll love" with the SlimeLog tagline "Rate it. Log it. Love it." — magenta accent, semibold, same visual slot. Tighter and on-brand. |
| T100 | SlimesSplitPanel full-screen edit form | MEDIUM | READY | 2026-07-09. Currently centered modal (Option B); Option C = full-screen edit page on mobile. Post-audit polish. |
| T101 | Slime detail card load perf | MEDIUM | DONE (server-side) | 2026-07-10: (a) wrapped fetchLog in React `cache()` so generateMetadata + page body share one query result instead of two. (b) Parallelized the follow-up fan-out (owner + brand + likes count + user-like + log_tags) into a single Promise.all instead of a 5-step serial waterfall. DevTools also revealed client-side duplication (see T104). |
| T104 | Client-side auth.getUser duplication | MEDIUM | DONE | 2026-07-10: shipped `<AuthProvider>` context in components/AuthProvider.tsx that fetches user + profile once on mount and syncs via onAuthStateChange. Refactored components: ShareButton, OnboardingGate, OnboardingModal, SlimeMenu, ClientComments. Refactored pages: /settings, /settings/profile, /invite, /welcome. Profile payload includes: id, username, avatar_url, display_name, role, referral_code, onboarding_completed_at, subscription_tier, is_premium, marketing_consent — future consumers add columns here rather than fanning out new queries. /invite still fetches referral_activations + pro_credit_months separately because those change while the user is on the page. /settings/profile keeps its heavy per-page profile fetch (social handles + form fields not in AuthProvider). |
| T105 | Inline app header (drop fixed dark bar) | MEDIUM | DEFERRED | 2026-07-11. Design mockup for feed rework shows an inline wordmark + notification bell at top of scroll content, no fixed PageHeader. Would kill the black `#0A0A0A` bar that feels utility-glued-on top of the neon-purple content. Needs: relocate hamburger contents (Invite Friends, Settings, admin link, sign out) to a redesigned /profile page acting as a "You" hub, or add a Profile bottom nav tab. Big scope — touches every page. Deferred until feed rework batches 2 + 3 land. Option A (restyle current bar transparent) is the short-term fix. |
| T106 | Founder badge for first 500 users | MEDIUM | READY | 2026-07-11. CommunityStatsHero ribbon currently hard-wires "You're one of the first 500 users ✦" while total users < 500 (pre-launch). Before we cross 500 we need: (a) profiles.is_founder boolean column, (b) backfill for existing users by created_at rank, (c) signup path sets it when current count < 500, (d) AuthProvider surfaces the flag, (e) ribbon reads from that flag so the badge sticks with the original 500 permanently. Follow-up = actual visible badge on the profile card. |
| T107 | Biggest galaxies leaderboard | MEDIUM | MOSTLY DONE | 2026-07-11. Rank users by log count per brand ("Top collectors of Cloud Nine"). Ships as: (a) new `/leaderboard` page with brand selector + ranked list of top N users, (b) "Top collectors" strip on `/brands/[slug]` showing the top 5 for that brand, (c) pill link on `/collection` (My Shelf) leading to `/leaderboard`, (d) later — earned badges on `/users/[username]` for "Top 5 Cloud Nine collector ✦" or similar. Data is a single aggregate query per brand: SELECT user_id, COUNT(*) FROM collection_logs WHERE brand_name_raw = X GROUP BY user_id ORDER BY count DESC. No schema changes needed. Design brief handed off to Claude Design 2026-07-11. |
| T110 | Community brand suggestion pipeline | HIGH | DONE | 2026-07-11. Users can suggest slime shops not yet in the catalog; admin approves → brand joins the catalog → submitter gets an in-app notification. Ships as: (a) `brand_suggestions` table + RLS, (b) `POST /api/brand-suggestions` submission endpoint with rate limit (1 per user per day + duplicate name/slug check), (c) `/submit-brand` dedicated form page, (d) entry from Discover page ("Know a brand we should track?"), (e) log-wizard "Not seeing your brand? Submit it →" fallback when BrandSearchInput returns zero matches, (f) `/admin/brand-suggestions` queue with approve/reject/duplicate actions, (g) approve creates a `brands` row and fires a `brand_suggestion_approved` notification, (h) new `notification_type` enum values + rendering. Push notifications are out of scope (T… TBD) — in-app inbox only for now. |
| T113 | Marketplace Coming Soon page + waitlist | HIGH | DONE | 2026-07-12. Shipped `/marketplace` — auth-gated waitlist page. Signed-out users bounce to `/login?next=/marketplace`; signed-in users see the hero + intent picker (sell / buy / both) + one-tap "Join the waitlist" CTA. After joining, the page flips to the success state: gradient position number "#{n}", "tell your slime friends" share CTA (Web Share API + clipboard fallback, appends `?ref=CODE` for referral credit), and an expandable "help us build this right" panel with 4 auto-saving research questions (brand multi-select from top 12 catalog brands, buyer spend band, seller monthly volume, 200-char trust-need free text). Returning users hydrate straight into the success state with prior answers pre-filled. Migration `20260712000068_marketplace_waitlist.sql` — `marketplace_waitlist` table with UNIQUE(user_id), intent CHECK enum, optional research columns, created_at index for position calc, RLS (INSERT/SELECT/UPDATE own row + admin SELECT/DELETE), `set_updated_at` trigger. Routes: `POST /api/marketplace/waitlist` (upserts on user_id conflict so users can layer research answers on top of intent, admin-client position + total, moderation gate on trust_need), `GET /api/marketplace/waitlist/position` (hydration lookup). Entry point: subtle magenta "Marketplace coming soon" pill above "My Stuff" in `SlimeMenu.tsx`, signed-in-only. Types added to `lib/types.ts`. Anti-AI-art: line SVG icons only, ambient gradient orbs, no mascots. |
| T113a | Marketplace launch trigger | MEDIUM | READY | 2026-07-12. When the marketplace ships, iterate `marketplace_waitlist` ordered by `created_at asc` and email the queue in position order. First N sellers get a launch tier bump (waived listing fee for the first month, or similar) as a thanks for being early — details TBD when we scope the marketplace itself. Uses `profiles.marketing_consent` as the send gate (T#17 hook) since the marketplace announcement is transactional-adjacent but the follow-on drip is marketing. Needs Brevo template + admin-triggered batch job. |
| T114 | Slime condition field on logs | LOW | DONE | 2026-07-12. Added `condition` enum column to collection_logs (sealed / new / like_new / used / well_loved). Migration 20260712000067. Wired into log wizard AND edit wizard as an optional pill picker sitting directly below scent strength (pill pickers grouped visually). Chip renders on SlimeDetailCard next to scent strength when set. Each pill has a hover title with a plain-English description. Serves two purposes: personal shelf tracking (users can note dried-out or sealed slimes) and future marketplace listing schema (T113 → full marketplace). |
| T115 | Rename user-facing "Subtype" → "Variant" | LOW | DONE | 2026-07-12. Jenn's guide V4 uses "Variants & Related" as the community's vocabulary for the styles that layer on top of a base texture (e.g. Glossy / Holographic on top of Clear). Renamed the user-facing label in log wizard + log edit wizard + SubtypeAutocomplete fallback placeholder. DB column stays `subtype_id` — internal-only rename would be cost with no user benefit. Component filename (`SubtypeAutocomplete.tsx`) also unchanged. |
| T112 | Brand Scout badges on profile | LOW | BACKLOG | 2026-07-11. Surface a Brand Scout badge on `/users/[username]` when a user has approved brand suggestions. Data already tracked via `profiles.approved_brand_suggestions_count` (migration 0066, T110 follow-up). Tiers: ✧ Brand Scout at 1+ approved, ✦ Brand Scout at 5+, ✧✦ Head Scout at 15+. Small pill or icon on the profile card. Waits for the profile redesign. |
| T111a | Retroactive moderation scan of existing user-generated content | LOW | BACKLOG | 2026-07-12. T111 covers new writes only. Existing rows in `profiles.username / bio / location`, `collection_logs.slime_name / brand_name_raw / collection_name / notes`, `tags.name`, `comments.body`, `brand_suggestions.name / note`, and `reports.reason` were written before the gate existed. Build a one-shot script (or nightly job) that runs `moderateText` over each row and files failing rows into an admin remediation queue — do NOT auto-null user data. Punted from T111 launch scope. |
| T29a | Realtime notification push (Supabase Realtime subscription in NotificationBell) | LOW | DEFERRED | 2026-07-12. NotificationBell currently polls `/api/notifications/unread-count` every 60s + on window focus. Follow-up = subscribe to `postgres_changes` on `public.notifications` filtered by `recipient_id = auth.uid()`, drop the interval, keep the focus refetch as a belt-and-suspenders reconciliation. Requires enabling Realtime on the table + confirming RLS-aware channel behavior. Not needed pre-launch — 60s cadence is fine for the drop-alert use case. |
| T29b | Wire remaining upstream notification inserts | MEDIUM | READY | 2026-07-12. The notification reader (T29) renders 10 enum types but only 2 are actually INSERT'd today: `brand_suggestion_approved` + `brand_suggestion_rejected` (from `/api/admin/brand-suggestions/[id]/{approve,reject}`). The other 8 — `drop_announced`, `drop_live`, `drop_sold_out`, `new_follower`, `friend_log`, `friend_rating`, `comment_on_log`, `like_on_log` — need writers. Likely a mix of triggers (drops, follows, likes, comments) and existing route hooks. Sequence: (a) follows/likes/comments are cheap-and-obvious triggers on their tables, (b) drop_* triggers on `drops` status transitions, (c) friend_log + friend_rating trigger on `collection_logs` with a follower fanout. Also needs a notification-preferences UI so users can mute categories. |
| T111 | Content moderation for user-generated names + submissions | MEDIUM | IN PROGRESS | 2026-07-12. Server-side moderation gate now shared across every user-authored text entry point. `lib/moderation.ts` exposes `moderateText(input, field)` returning `{ ok, cleaned }` or `{ ok: false, reason, message }` (see file for the field list). Wired into: (a) `lib/profile-actions.ts` — username / bio / location on both settings + onboarding paths + live availability check, (b) `POST /api/brand-suggestions` — brand name + note, (c) `lib/slime-actions.ts` — slime name / collection name / brand-name-raw / notes / each keyword on both create + edit, (d) `POST /api/report` — reason, (e) NEW `POST /api/comments` — comment body (CommentSection.tsx refactored to call this route instead of writing directly through the anon client). Profanity check uses `obscenity` (RegExpMatcher + English dataset + recommended transformers) — handles leet / spacing / mixed case. Reserved-username list is baked in (admin, official, slimelog, jennifer, etc.). Admin-authored fields (admin_notes on brand_suggestions + brand_claims) are exempt by design. Retroactive scan of existing rows deferred to T111a. Follow-up to T110. Requires `npm install` for the new `obscenity` dep. |
| T109 | Custom champion titles as brand feature | LOW | BACKLOG | 2026-07-11. On `/leaderboard`, the #1 collector of a brand gets a "champion" badge. V1 auto-generates the title as "The {brand} Champion". Later, we let claimed brands set their own custom title as a paid perk (part of brand PRO tier) — e.g. Cloud Nine picks "The Butter Queen" instead of "The Cloud Nine Champion". Add a `brands.champion_title text` column when we build this, guarded by tier check. Fun / low-stakes. Depends on brand claim flow being live. |
| T108 | Brand logos in GalaxyView hubs | MEDIUM | DONE | 2026-07-11. Each brand hub in `/collection` Galaxy view now renders the real brand logo when `brands.logo_url` is set, falling back to the deterministic color puck when it isn't. Impl: per-brand `.ilike()` lookup against brands table on mount, async Image loading with `crossOrigin=anonymous`, circular-clipped `object-fit: cover` drawImage inside the hub circle. Color puck stays as an underlay so transparent PNGs read clean and hub sizing stays identical whether the image loaded or not. Progressive: as brands claim their profile and upload logos, existing users' galaxies get richer without any code change. Legend copy updated to "Hub grows with brand depth · logo when brand joins". |
| T102 | Admin subscription toggle | MEDIUM | DONE | 2026-07-11: /admin/subscriptions page + /api/admin/subscriptions/set-tier route. Admin can flip a user's or brand's subscription_tier + subscription_status directly (bypasses Stripe entirely). Coerces to null status when tier=free. Warning banner reminds this bypasses Stripe. |
| T103 | Sync-from-Stripe admin escape hatch | MEDIUM | DONE | 2026-07-11: /api/admin/subscriptions/sync-from-stripe route + button on /admin/subscriptions. Queries stripe.subscriptions.list for the row's stripe_customer_id, picks the most authoritative sub (active > trialing > past_due > canceled etc), and overwrites subscription_tier + status + period_end + cancel_at_period_end. If Stripe reports no subs, downgrades to free/null. Uses admin client so bypasses HP-8 protect trigger. |

---

## Post-Launch Nice-to-Have

| # | Issue | Priority | Status | Notes |
| --- | --- | --- | --- | --- |
| 8 | Badge system | POST-LAUNCH | DEFERRED | Untappd-style achievements. Pairs with T35. |
| 9 | Fullscreen canvas fix | POST-LAUNCH | DEFERRED | SpiralView/GalaxyView black canvas on fullscreen. |
| 10 | Comment replies | POST-LAUNCH | DEFERRED | V2 feature. |
| 18 | Ad consent toggle | POST-LAUNCH | DEFERRED | Opt-out toggle when ads added. |
| 36 | Open home feed to logged-out users | POST-LAUNCH | DEFERRED | Fast-follow #35. |
| T28 | Brand page expanded sections | POST-LAUNCH | DEFERRED | Past drops, favorites, all drops. |
| T49 | Bottom nav: Log → Brand for verified owners | POST-LAUNCH | DEFERRED | Chat 9. Multi-brand-owner question needs design conversation. |
| T54 | Under Review intermediate claim state | POST-LAUNCH | DEFERRED | Chat 9. For multi-admin scenarios. Adds new enum value + reviewing_by field. |

---

## Technical Debt

| # | Issue | Priority | Status | Notes |
| --- | --- | --- | --- | --- |
| T2 | Discover page threshold | TECH DEBT | DONE | Verified 2026-07-11: /discover top-rated slimes query already uses `.gte("total_ratings", 3)` (page.tsx line 41). No other discover subpage filters by rating count. |
| T3 | Brand association for legacy logs | TECH DEBT | DEFERRED | No brand_id FK on pre-BrandSearchInput logs. |
| T4 | Recharts width/height warnings | TECH DEBT | DEFERRED | width(-1)/height(-1) in dev console. |
| T7 | Admin reports RLS | TECH DEBT | READY | Allows all auth users to read reports. |
| T8 | Spiral and Galaxy drag/zoom | TECH DEBT | DEFERRED | Not functioning correctly. |
| T9 | brands RLS always true | TECH DEBT | READY | Tighten now that brand claiming (#3) is shipped — owner_id is the gate. |
| T14 | Vercel env var env separation | TECH DEBT | DEFERRED | Separate live from test. Post-launch. |
| T16 | Canonical domain audit (apex vs www) | TECH DEBT | DEFERRED | robots.ts and sitemap.ts use apex. |
| T17 | Brevo attribute prefixing | TECH DEBT | DEFERRED | Filed during chat 7. |
| T18 | Public route rate limiting | TECH DEBT | DEFERRED | Defer post-launch. |
| T19 | Migration sequence collision | TECH DEBT | READY | 20260420000030 vs 20260423000030. Plus chat 9 noted handoff doc lists wrong filenames vs disk. |
| T20 | BottomNavWrapper hydration flash | TECH DEBT | DEFERRED | Acknowledged. |
| T21 | activity_feed trigger fires on private logs | TECH DEBT | DEFERRED | Privacy edge case. |
| T22 | Feed-detail state sync | TECH DEBT | DEFERRED | Likes/comments sync between feed cards and detail overlay. |
| T23 | FollowBrandButton Following state | TECH DEBT | DEFERRED | Visual issue on touch devices. |
| T24 | PageHeader uses <img> not next/image | TECH DEBT | DEFERRED | Defer. |
| T25 | PageHeader profile button avatar | TECH DEBT | DEFERRED | Could show actual user avatar. |
| T26 | Brand page empty state polish | TECH DEBT | DEFERRED | Post-launch. |
| T27 | Public/private toggle UI for log creation | TECH DEBT | READY | Required to verify privacy bug fix from #35. |
| T32 | Optimize robots.txt for LLM crawlers | TECH DEBT | DEFERRED | GPTBot, Google-Extended, ClaudeBot, etc. |
| T33 | Add JSON-LD structured data | TECH DEBT | DEFERRED | Schema.org Review/Person/Organization/Event. |
| T34 | Audit sitemap caching | TECH DEBT | DEFERRED | Add revalidate or force-static post-launch. |
| T37 | Address consistency audit post-May 6 | TECH DEBT | READY | After CT finalization, audit all vendor accounts have Glastonbury address. |
| T38 | DBA filing decision | TECH DEBT | DEFERRED | SlimeLog DBA decision deferred to pre-launch. |
| T39 | Migration filename audit (chat 9) | TECH DEBT | READY | Handoff doc migration table lists names that don't match disk. Reconcile pre-launch. |
| T40 | /admin/waitlist refactor to createAdminClient | TECH DEBT | READY | Chat 9. Use the centralized service-role factory introduced in #3. |
| T41 | Resend client consolidation | TECH DEBT | READY | Chat 9. Move inline new Resend(...) calls to lib/email.ts. |
| T43 | Resend code path simplification | TECH DEBT | READY | Chat 9. /api/brand-claims/submit resend should re-fetch claim, not re-validate all fields. |
| T46 | Brand website_url scrub pre-launch | TECH DEBT | READY | Chat 9. Audit brands table to maximize domain-match coverage during claim verification. |
| T50 | Terminal/chat-UI auto-link rendering artifacts (DOCUMENTED) | TECH DEBT | DOCUMENTED | Chat 9. [process.env.X.Y](http://...) patterns in cat output are display-only, not real corruption. Verify with od -c or trust tsc. |
| T53 | Admin landing page | TECH DEBT | DEFERRED | Chat 9. /admin back button currently 404s. Resolves when /admin/page.tsx exists. |
| T55 | Standardized rejection reason dropdown (alias for T51) | TECH DEBT | READY | Same item as T51 — relabeled for consistency. |

---

## Non-Code Action Items

| # | Item | Priority | Status | Notes |
| --- | --- | --- | --- | --- |
| A1 | DMCA agent registration | MEDIUM | READY | copyright.gov/dmca-directory — $6, ~10 min. Use Glastonbury address post-May 6. |
| A2 | Rotate all API keys before go-live | MEDIUM | READY | Supabase, Resend, Stripe, Brevo. |
| A3 | Leaked Password Protection | LOW | BLOCKED | Requires Supabase Pro plan. |
| A6 | Prevent duplicate subscriptions | MEDIUM | READY | See #21. |
| A9 | Supabase ownership transfer | MEDIUM | IN PROGRESS | support@chaphaus.com as Owner. |
| A10 | Google OAuth project transfer | MEDIUM | BLOCKED | After A9. |
| A11 | GitHub org creation | MEDIUM | BLOCKED | After A10. |
| A12 | Vercel reconnection | MEDIUM | BLOCKED | After A11. |
| A13 | Resend account transfer | MEDIUM | BLOCKED | After A11. |
| A14 | Instagram @SlimeLogApp admin | LOW | READY | Add support@slimelog.com as admin. |
| A15 | DMCA agent renewal | POST-LAUNCH | DEFERRED | April 2029 (3-year cycle). |
| A16 | Apple Developer enrollment | HIGH | BLOCKED | Blocked on D&B address propagation post-May 6 CT SOS finalization. |
| A17 | Address propagation across vendor accounts | HIGH | IN PROGRESS | Stripe + Bluevine done. Pending: D&B, IRS Form 8822-B, Brevo, Resend, Supabase, Vercel, chaphaus.com. |
| A18 | CT Change of Agent finalization | HIGH | IN PROGRESS | Filed at CT SOS, est. completion May 6, 2026. Required before A17. |

---

## Completed — Recent (Preserved for History)

*Items shipped, kept here as institutional memory.*

| # | Issue | Closed In | Status | Notes |
| --- | --- | --- | --- | --- |
| #3 | Brand claiming flow | Chat 9 (May 4) | DONE | 3 batches, 17 new files, migration 33. End-to-end: claim form + email verification + document upload + admin queue + per-claim review with inline document preview + approve/reject + auto-rejection cascade. |
| T11 | Comment relative time | Chat 9 (May 4) | DONE | Already rendering '29d ago' format correctly. |
| T42 | PageHeader BACK_BUTTON_ROUTES additions | Chat 9 (May 4) | DONE | Added /^\/brands\/[^/]+\/claim$/ and /^\/brand-verification$/. |
| T44 | tsconfig.tsbuildinfo to .gitignore | Chat 9 (May 4) | DONE | Added .gitignore entry, git rm --cached tracked copy. |
| T45 | Removed temporary inline back button hacks | Chat 9 (May 4) | DONE | Bundle A. brand-verification + brands/[slug]/claim now use PageHeader's back button. |
| T48 | Brand page Follow → Manage Brand for verified owner | Chat 9 (May 4) | DONE | Folded into Batch 3 of #3. Conditional on brand.owner_id === user.id. |
| #2 | Email infrastructure (Brevo) | Chat 8 (May 1) | DONE | Welcome email working end-to-end. |
| T30 | SlimeDetailCard log creator surfacing | Chat 8 (May 2) | DONE | Owner avatar + @username row at top of overlay. |
| T31 | Full review page back button + scroll restoration | Chat 8 (May 2) | DONE | sessionStorage nav stack, height-aware scroll restoration. |
| #35 | Public profile access (logged-out browsing) | Chat 7 (Apr 28) | DONE | 10 batches, 28 files, 4 hotfixes. |
| A8 | Stripe live real-card test | Chat 7 (Apr 27) | DONE | Real $2.99 purchase end-to-end. |
| A4 | Stripe live mode setup | Chat 6 (Apr 23) | DONE | Products, webhook, customer portal, branding. |
| A5 | Stripe portal branding | Chat 6 (Apr 23) | DONE | ChapHaus business name, SlimeLog logo. |
| A7 | ChapHaus website launch | Chat 6 (Apr 23) | DONE | Live at chaphaus.com. |
| #24 | COPPA age gate UX fix | Chat 6 (Apr 23) | DONE | Under-13 redirect + COPPA data deletion. |
| #47 | Age verification DOB picker UX | Chat 6 (Apr 23) | DONE | Month/Day/Year dropdowns. |
| T13 | SlimeMenu module-level supabase client | Chat 6 (Apr 23) | DONE | Hoisted createBrowserClient. |

---

## Priority Heatmap — What to Tackle Next

### Pre-Launch Critical Path (in dependency order)

| # | Issue | Priority | Status | Why now |
| --- | --- | --- | --- | --- |
| A18 | CT Change of Agent finalization | CRITICAL | IN PROGRESS | May 6 estimated completion. Unblocks A17 address propagation. |
| A17 | Address propagation | CRITICAL | IN PROGRESS | After A18. Pending: D&B (Apple bottleneck), IRS, Brevo, Resend, Supabase, Vercel, chaphaus.com. |
| A16 | Apple Developer enrollment | CRITICAL | BLOCKED | Blocked on D&B address propagation (~2-4 weeks post-May 6). |
| #22 | RevenueCat integration | HIGH | BLOCKED | After A16. Required for App Store Pro tier. |
| #14 | Subscription terms display | HIGH | BLOCKED | Part of #22. |
| #23 | Capacitor packaging | HIGH | BLOCKED | After A16. |
| #26 | App Store review checklist | HIGH | IN PROGRESS | Final pre-submission audit. |
| T46 | Brand website_url scrub | HIGH | READY | Chat 9. Maximize domain-match coverage for brand claim verification before launch. Manual data work. |
| T9 | brands RLS tightening | HIGH | READY | Now that #3 is shipped, owner_id is the gate. Tighten the always-true RLS. |

### UX Quick Wins (small, ship anytime)

| # | Issue | Priority | Status | Effort |
| --- | --- | --- | --- | --- |
| T2 | Discover page threshold raise | LOW | READY | Tiny. Single SQL change. Write inline, apply directly. |
| #20 | Double toast on Pro upgrade fix | MEDIUM | READY | Small. Toast deduplication. Stripe-adjacent — careful smoke test. |
| #21 | Duplicate subscription guard | MEDIUM | READY | Small. Early return in checkout route. Stripe-adjacent. |
| T36 | Wizard step-1 escape behavior | MEDIUM | READY | Small. Back button on /log step 0 only. |
| T47 | Email editing during claim step 1 | MEDIUM | READY | Small. PATCH existing pending claim. Brand claiming polish. |
| T51 | Standardized rejection dropdown | MEDIUM | READY | Small. Replace free-text with 5 standardized reasons + Other. |
| T52 | Hide bottom nav on admin pages | MEDIUM | READY | Tiny. BottomNavWrapper detects /admin/*. |

### Recommended Next Action (post chat 9)

With #3 closed, the path forward divides into two streams:

1. While A18/A16 chain blocks Apple, knock down small items: T2 (single SQL), T36 (small UI), T9 (RLS tightening), T46 (manual data scrub).
2. Brand claiming polish bundle once we decide it's worth it: T47 + T51 + T52 + T53. All small, all related, ship as one batch.
3. Bundle B Stripe safety fixes (#20 + #21) — ship cautiously since Stripe is live.

---

## How This Tracker Works

This is the living source of truth for SlimeLog development. It is updated at the end of every master chat session. It pairs with the Master Development Handoff Document — the handoff is institutional memory; this is the to-do list.

### Update Rules

- APPEND ONLY for closed items — move to Completed section, do not delete.
- Status updates encouraged: READY → IN PROGRESS → DONE / BLOCKED / DEFERRED.
- New items append to the appropriate priority section.
- Items rarely change priority bucket — update Priority column rather than moving rows.
- Tracker numbering shared with handoff doc — `#` for product, `T` for tech debt, `A` for non-code.

### Status Definitions

- **READY** — Available to work on now. No blockers.
- **IN PROGRESS** — Active work threads exist.
- **BLOCKED** — Waiting on something external.
- **DONE** — Shipped and verified in production.
- **DEFERRED** — Not urgent. Post-launch or lower priority.

### Pairing with Handoff Doc

The handoff doc captures session-by-session changes. This tracker captures forward-looking work. Both append-only. Both versioned per master chat.

---

*SlimeLog — Rate it. Log it. Love it.*

*CONFIDENTIAL — For Development Use Only*
